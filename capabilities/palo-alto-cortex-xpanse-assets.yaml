naftiko: 1.0.0-alpha2
info:
  label: Palo Alto Networks Cortex Xpanse REST API — Assets
  description: 'Palo Alto Networks Cortex Xpanse REST API — Assets. 2 operations. Lead operation: Palo Alto Networks Get Single
    Asset Details. Self-contained Naftiko capability covering one Palo Alto Networks business surface.'
  tags:
  - Palo Alto Networks
  - Assets
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PALO_ALTO_NETWORKS_API_KEY: PALO_ALTO_NETWORKS_API_KEY
capability:
  consumes:
  - type: http
    namespace: palo-alto-cortex-xpanse-assets
    baseUri: https://api-{fqdn}/public_api/v1
    description: Palo Alto Networks Cortex Xpanse REST API — Assets business capability. Self-contained, no shared references.
    resources:
    - name: assets-get_asset_internet_exposure
      path: /assets/get_asset_internet_exposure
      operations:
      - name: getassetinternetexposure
        method: POST
        description: Palo Alto Networks Get Single Asset Details
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: assets-get_assets_internet_exposure
      path: /assets/get_assets_internet_exposure
      operations:
      - name: getassetsinternetexposure
        method: POST
        description: Palo Alto Networks Get Internet-exposed Assets
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: apikey
      key: x-xdr-hmac-v2
      value: '{{env.PALO_ALTO_NETWORKS_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: palo-alto-cortex-xpanse-assets-rest
    port: 8080
    description: REST adapter for Palo Alto Networks Cortex Xpanse REST API — Assets. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/assets/get-asset-internet-exposure
      name: assets-get-asset-internet-exposure
      description: REST surface for assets-get_asset_internet_exposure.
      operations:
      - method: POST
        name: getassetinternetexposure
        description: Palo Alto Networks Get Single Asset Details
        call: palo-alto-cortex-xpanse-assets.getassetinternetexposure
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/assets/get-assets-internet-exposure
      name: assets-get-assets-internet-exposure
      description: REST surface for assets-get_assets_internet_exposure.
      operations:
      - method: POST
        name: getassetsinternetexposure
        description: Palo Alto Networks Get Internet-exposed Assets
        call: palo-alto-cortex-xpanse-assets.getassetsinternetexposure
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: palo-alto-cortex-xpanse-assets-mcp
    port: 9090
    transport: http
    description: MCP adapter for Palo Alto Networks Cortex Xpanse REST API — Assets. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: palo-alto-networks-get-single
      description: Palo Alto Networks Get Single Asset Details
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: palo-alto-cortex-xpanse-assets.getassetinternetexposure
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: palo-alto-networks-get-internet
      description: Palo Alto Networks Get Internet-exposed Assets
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: palo-alto-cortex-xpanse-assets.getassetsinternetexposure
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
