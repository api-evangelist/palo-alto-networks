naftiko: 1.0.0-alpha2
info:
  label: Palo Alto Networks Prisma Cloud CSPM API — Compliance
  description: 'Palo Alto Networks Prisma Cloud CSPM API — Compliance. 1 operations. Lead operation: Palo Alto Networks List
    Compliance Standards. Self-contained Naftiko capability covering one Palo Alto Networks business surface.'
  tags:
  - Palo Alto Networks
  - Compliance
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PALO_ALTO_NETWORKS_API_KEY: PALO_ALTO_NETWORKS_API_KEY
capability:
  consumes:
  - type: http
    namespace: palo-alto-prisma-cloud-cspm-compliance
    baseUri: https://api.prismacloud.io
    description: Palo Alto Networks Prisma Cloud CSPM API — Compliance business capability. Self-contained, no shared references.
    resources:
    - name: compliance
      path: /compliance
      operations:
      - name: listcompliancestandards
        method: GET
        description: Palo Alto Networks List Compliance Standards
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.PALO_ALTO_NETWORKS_API_KEY}}'
  exposes:
  - type: rest
    namespace: palo-alto-prisma-cloud-cspm-compliance-rest
    port: 8080
    description: REST adapter for Palo Alto Networks Prisma Cloud CSPM API — Compliance. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/compliance
      name: compliance
      description: REST surface for compliance.
      operations:
      - method: GET
        name: listcompliancestandards
        description: Palo Alto Networks List Compliance Standards
        call: palo-alto-prisma-cloud-cspm-compliance.listcompliancestandards
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: palo-alto-prisma-cloud-cspm-compliance-mcp
    port: 9090
    transport: http
    description: MCP adapter for Palo Alto Networks Prisma Cloud CSPM API — Compliance. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: palo-alto-networks-list-compliance
      description: Palo Alto Networks List Compliance Standards
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: palo-alto-prisma-cloud-cspm-compliance.listcompliancestandards
      outputParameters:
      - type: object
        mapping: $.
