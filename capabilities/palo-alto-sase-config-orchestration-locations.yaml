naftiko: 1.0.0-alpha2
info:
  label: Palo Alto Networks SASE Configuration Orchestration API — Locations
  description: 'Palo Alto Networks SASE Configuration Orchestration API — Locations. 1 operations. Lead operation: Palo Alto
    Networks List Prisma Access Locations. Self-contained Naftiko capability covering one Palo Alto Networks business surface.'
  tags:
  - Palo Alto Networks
  - Locations
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PALO_ALTO_NETWORKS_API_KEY: PALO_ALTO_NETWORKS_API_KEY
capability:
  consumes:
  - type: http
    namespace: palo-alto-sase-config-orchestration-locations
    baseUri: https://api.sase.paloaltonetworks.com/config-orchestration/v1
    description: Palo Alto Networks SASE Configuration Orchestration API — Locations business capability. Self-contained,
      no shared references.
    resources:
    - name: locations
      path: /locations
      operations:
      - name: listprismaaccesslocations
        method: GET
        description: Palo Alto Networks List Prisma Access Locations
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.PALO_ALTO_NETWORKS_API_KEY}}'
  exposes:
  - type: rest
    namespace: palo-alto-sase-config-orchestration-locations-rest
    port: 8080
    description: REST adapter for Palo Alto Networks SASE Configuration Orchestration API — Locations. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/locations
      name: locations
      description: REST surface for locations.
      operations:
      - method: GET
        name: listprismaaccesslocations
        description: Palo Alto Networks List Prisma Access Locations
        call: palo-alto-sase-config-orchestration-locations.listprismaaccesslocations
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: palo-alto-sase-config-orchestration-locations-mcp
    port: 9090
    transport: http
    description: MCP adapter for Palo Alto Networks SASE Configuration Orchestration API — Locations. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: palo-alto-networks-list-prisma
      description: Palo Alto Networks List Prisma Access Locations
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: palo-alto-sase-config-orchestration-locations.listprismaaccesslocations
      outputParameters:
      - type: object
        mapping: $.
