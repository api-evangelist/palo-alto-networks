naftiko: 1.0.0-alpha2
info:
  label: Palo Alto Networks Prisma Access Insights API — Custom Queries
  description: 'Palo Alto Networks Prisma Access Insights API — Custom Queries. 1 operations. Lead operation: Palo Alto Networks
    Execute a Custom Data Resource Query. Self-contained Naftiko capability covering one Palo Alto Networks business surface.'
  tags:
  - Palo Alto Networks
  - Custom Queries
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PALO_ALTO_NETWORKS_API_KEY: PALO_ALTO_NETWORKS_API_KEY
capability:
  consumes:
  - type: http
    namespace: palo-alto-prisma-access-insights-custom-queries
    baseUri: https://api.sase.paloaltonetworks.com
    description: Palo Alto Networks Prisma Access Insights API — Custom Queries business capability. Self-contained, no shared
      references.
    resources:
    - name: insights-v3.0-resource-custom-query
      path: /insights/v3.0/resource/custom-query
      operations:
      - name: executecustomquery
        method: POST
        description: Palo Alto Networks Execute a Custom Data Resource Query
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.PALO_ALTO_NETWORKS_API_KEY}}'
  exposes:
  - type: rest
    namespace: palo-alto-prisma-access-insights-custom-queries-rest
    port: 8080
    description: REST adapter for Palo Alto Networks Prisma Access Insights API — Custom Queries. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/insights/v3-0/resource/custom-query
      name: insights-v3-0-resource-custom-query
      description: REST surface for insights-v3.0-resource-custom-query.
      operations:
      - method: POST
        name: executecustomquery
        description: Palo Alto Networks Execute a Custom Data Resource Query
        call: palo-alto-prisma-access-insights-custom-queries.executecustomquery
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: palo-alto-prisma-access-insights-custom-queries-mcp
    port: 9090
    transport: http
    description: MCP adapter for Palo Alto Networks Prisma Access Insights API — Custom Queries. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: palo-alto-networks-execute-custom
      description: Palo Alto Networks Execute a Custom Data Resource Query
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: palo-alto-prisma-access-insights-custom-queries.executecustomquery
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
