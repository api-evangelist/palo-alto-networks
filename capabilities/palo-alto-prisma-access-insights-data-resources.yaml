naftiko: 1.0.0-alpha2
info:
  label: Palo Alto Networks Prisma Access Insights API — Data Resources
  description: 'Palo Alto Networks Prisma Access Insights API — Data Resources. 1 operations. Lead operation: Palo Alto Networks
    Query a Data Resource. Self-contained Naftiko capability covering one Palo Alto Networks business surface.'
  tags:
  - Palo Alto Networks
  - Data Resources
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PALO_ALTO_NETWORKS_API_KEY: PALO_ALTO_NETWORKS_API_KEY
capability:
  consumes:
  - type: http
    namespace: palo-alto-prisma-access-insights-data-resources
    baseUri: https://api.sase.paloaltonetworks.com
    description: Palo Alto Networks Prisma Access Insights API — Data Resources business capability. Self-contained, no shared
      references.
    resources:
    - name: insights-v3.0-resource-query-resource_name
      path: /insights/v3.0/resource/query/{resource_name}
      operations:
      - name: querydataresource
        method: POST
        description: Palo Alto Networks Query a Data Resource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.PALO_ALTO_NETWORKS_API_KEY}}'
  exposes:
  - type: rest
    namespace: palo-alto-prisma-access-insights-data-resources-rest
    port: 8080
    description: REST adapter for Palo Alto Networks Prisma Access Insights API — Data Resources. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/insights/v3-0/resource/query/{resource-name}
      name: insights-v3-0-resource-query-resource-name
      description: REST surface for insights-v3.0-resource-query-resource_name.
      operations:
      - method: POST
        name: querydataresource
        description: Palo Alto Networks Query a Data Resource
        call: palo-alto-prisma-access-insights-data-resources.querydataresource
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: palo-alto-prisma-access-insights-data-resources-mcp
    port: 9090
    transport: http
    description: MCP adapter for Palo Alto Networks Prisma Access Insights API — Data Resources. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: palo-alto-networks-query-data
      description: Palo Alto Networks Query a Data Resource
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: palo-alto-prisma-access-insights-data-resources.querydataresource
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
