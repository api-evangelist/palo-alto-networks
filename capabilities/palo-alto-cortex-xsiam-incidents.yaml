naftiko: 1.0.0-alpha2
info:
  label: Palo Alto Networks Cortex XSIAM REST API — Incidents
  description: 'Palo Alto Networks Cortex XSIAM REST API — Incidents. 1 operations. Lead operation: Palo Alto Networks List
    Incidents. Self-contained Naftiko capability covering one Palo Alto Networks business surface.'
  tags:
  - Palo Alto Networks
  - Incidents
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PALO_ALTO_NETWORKS_API_KEY: PALO_ALTO_NETWORKS_API_KEY
capability:
  consumes:
  - type: http
    namespace: palo-alto-cortex-xsiam-incidents
    baseUri: https://api-{fqdn}/public_api/v1
    description: Palo Alto Networks Cortex XSIAM REST API — Incidents business capability. Self-contained, no shared references.
    resources:
    - name: incidents-get_incidents
      path: /incidents/get_incidents
      operations:
      - name: getincidents
        method: POST
        description: Palo Alto Networks List Incidents
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: apikey
      key: x-xdr-hmac-v2
      value: '{{env.PALO_ALTO_NETWORKS_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: palo-alto-cortex-xsiam-incidents-rest
    port: 8080
    description: REST adapter for Palo Alto Networks Cortex XSIAM REST API — Incidents. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/incidents/get-incidents
      name: incidents-get-incidents
      description: REST surface for incidents-get_incidents.
      operations:
      - method: POST
        name: getincidents
        description: Palo Alto Networks List Incidents
        call: palo-alto-cortex-xsiam-incidents.getincidents
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: palo-alto-cortex-xsiam-incidents-mcp
    port: 9090
    transport: http
    description: MCP adapter for Palo Alto Networks Cortex XSIAM REST API — Incidents. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: palo-alto-networks-list-incidents
      description: Palo Alto Networks List Incidents
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: palo-alto-cortex-xsiam-incidents.getincidents
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
