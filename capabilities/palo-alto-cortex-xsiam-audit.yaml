naftiko: 1.0.0-alpha2
info:
  label: Palo Alto Networks Cortex XSIAM REST API — Audit
  description: 'Palo Alto Networks Cortex XSIAM REST API — Audit. 1 operations. Lead operation: Palo Alto Networks Get Audit
    Management Logs. Self-contained Naftiko capability covering one Palo Alto Networks business surface.'
  tags:
  - Palo Alto Networks
  - Audit
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PALO_ALTO_NETWORKS_API_KEY: PALO_ALTO_NETWORKS_API_KEY
capability:
  consumes:
  - type: http
    namespace: palo-alto-cortex-xsiam-audit
    baseUri: https://api-{fqdn}/public_api/v1
    description: Palo Alto Networks Cortex XSIAM REST API — Audit business capability. Self-contained, no shared references.
    resources:
    - name: audits-management_logs-get_management_logs
      path: /audits/management_logs/get_management_logs
      operations:
      - name: getmanagementlogs
        method: POST
        description: Palo Alto Networks Get Audit Management Logs
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: apikey
      key: x-xdr-hmac-v2
      value: '{{env.PALO_ALTO_NETWORKS_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: palo-alto-cortex-xsiam-audit-rest
    port: 8080
    description: REST adapter for Palo Alto Networks Cortex XSIAM REST API — Audit. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/audits/management-logs/get-management-logs
      name: audits-management-logs-get-management-logs
      description: REST surface for audits-management_logs-get_management_logs.
      operations:
      - method: POST
        name: getmanagementlogs
        description: Palo Alto Networks Get Audit Management Logs
        call: palo-alto-cortex-xsiam-audit.getmanagementlogs
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: palo-alto-cortex-xsiam-audit-mcp
    port: 9090
    transport: http
    description: MCP adapter for Palo Alto Networks Cortex XSIAM REST API — Audit. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: palo-alto-networks-get-audit
      description: Palo Alto Networks Get Audit Management Logs
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: palo-alto-cortex-xsiam-audit.getmanagementlogs
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
