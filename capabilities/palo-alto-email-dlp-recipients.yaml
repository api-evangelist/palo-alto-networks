naftiko: 1.0.0-alpha2
info:
  label: Palo Alto Networks Email DLP API — Recipients
  description: 'Palo Alto Networks Email DLP API — Recipients. 1 operations. Lead operation: Palo Alto Networks Get Email
    Incident Recipients. Self-contained Naftiko capability covering one Palo Alto Networks business surface.'
  tags:
  - Palo Alto Networks
  - Recipients
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PALO_ALTO_NETWORKS_API_KEY: PALO_ALTO_NETWORKS_API_KEY
capability:
  consumes:
  - type: http
    namespace: palo-alto-email-dlp-recipients
    baseUri: https://api.{region}.dlp.paloaltonetworks.com/v1
    description: Palo Alto Networks Email DLP API — Recipients business capability. Self-contained, no shared references.
    resources:
    - name: email-incidents-id-recipients
      path: /email-incidents/{id}/recipients
      operations:
      - name: getemailincidentrecipients
        method: GET
        description: Palo Alto Networks Get Email Incident Recipients
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: id
          in: path
          type: string
          description: Unique incident identifier.
          required: true
    authentication:
      type: bearer
      token: '{{env.PALO_ALTO_NETWORKS_API_KEY}}'
  exposes:
  - type: rest
    namespace: palo-alto-email-dlp-recipients-rest
    port: 8080
    description: REST adapter for Palo Alto Networks Email DLP API — Recipients. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/email-incidents/{id}/recipients
      name: email-incidents-id-recipients
      description: REST surface for email-incidents-id-recipients.
      operations:
      - method: GET
        name: getemailincidentrecipients
        description: Palo Alto Networks Get Email Incident Recipients
        call: palo-alto-email-dlp-recipients.getemailincidentrecipients
        with:
          id: rest.id
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: palo-alto-email-dlp-recipients-mcp
    port: 9090
    transport: http
    description: MCP adapter for Palo Alto Networks Email DLP API — Recipients. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: palo-alto-networks-get-email
      description: Palo Alto Networks Get Email Incident Recipients
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: palo-alto-email-dlp-recipients.getemailincidentrecipients
      with:
        id: tools.id
      outputParameters:
      - type: object
        mapping: $.
