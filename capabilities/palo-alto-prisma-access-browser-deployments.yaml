naftiko: 1.0.0-alpha2
info:
  label: Palo Alto Networks Prisma Access Browser Management API — Deployments
  description: 'Palo Alto Networks Prisma Access Browser Management API — Deployments. 5 operations. Lead operation: Palo
    Alto Networks List Browser Deployments. Self-contained Naftiko capability covering one Palo Alto Networks business surface.'
  tags:
  - Palo Alto Networks
  - Deployments
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PALO_ALTO_NETWORKS_API_KEY: PALO_ALTO_NETWORKS_API_KEY
capability:
  consumes:
  - type: http
    namespace: palo-alto-prisma-access-browser-deployments
    baseUri: https://api.sase.paloaltonetworks.com/browser-mgmt
    description: Palo Alto Networks Prisma Access Browser Management API — Deployments business capability. Self-contained,
      no shared references.
    resources:
    - name: v1-deployments
      path: /v1/deployments
      operations:
      - name: listdeployments
        method: GET
        description: Palo Alto Networks List Browser Deployments
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: createdeployment
        method: POST
        description: Palo Alto Networks Create Browser Deployment
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: v1-deployments-deployment_id
      path: /v1/deployments/{deployment_id}
      operations:
      - name: getdeployment
        method: GET
        description: Palo Alto Networks Get Browser Deployment
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: deployment_id
          in: path
          type: string
          description: Unique identifier of the deployment.
          required: true
      - name: updatedeployment
        method: PUT
        description: Palo Alto Networks Update Browser Deployment
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: deployment_id
          in: path
          type: string
          description: Unique identifier of the deployment to update.
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: deletedeployment
        method: DELETE
        description: Palo Alto Networks Delete Browser Deployment
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: deployment_id
          in: path
          type: string
          description: Unique identifier of the deployment to delete.
          required: true
    authentication:
      type: bearer
      token: '{{env.PALO_ALTO_NETWORKS_API_KEY}}'
  exposes:
  - type: rest
    namespace: palo-alto-prisma-access-browser-deployments-rest
    port: 8080
    description: REST adapter for Palo Alto Networks Prisma Access Browser Management API — Deployments. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/v1/deployments
      name: v1-deployments
      description: REST surface for v1-deployments.
      operations:
      - method: GET
        name: listdeployments
        description: Palo Alto Networks List Browser Deployments
        call: palo-alto-prisma-access-browser-deployments.listdeployments
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createdeployment
        description: Palo Alto Networks Create Browser Deployment
        call: palo-alto-prisma-access-browser-deployments.createdeployment
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/v1/deployments/{deployment-id}
      name: v1-deployments-deployment-id
      description: REST surface for v1-deployments-deployment_id.
      operations:
      - method: GET
        name: getdeployment
        description: Palo Alto Networks Get Browser Deployment
        call: palo-alto-prisma-access-browser-deployments.getdeployment
        with:
          deployment_id: rest.deployment_id
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: updatedeployment
        description: Palo Alto Networks Update Browser Deployment
        call: palo-alto-prisma-access-browser-deployments.updatedeployment
        with:
          deployment_id: rest.deployment_id
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deletedeployment
        description: Palo Alto Networks Delete Browser Deployment
        call: palo-alto-prisma-access-browser-deployments.deletedeployment
        with:
          deployment_id: rest.deployment_id
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: palo-alto-prisma-access-browser-deployments-mcp
    port: 9090
    transport: http
    description: MCP adapter for Palo Alto Networks Prisma Access Browser Management API — Deployments. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: palo-alto-networks-list-browser
      description: Palo Alto Networks List Browser Deployments
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: palo-alto-prisma-access-browser-deployments.listdeployments
      outputParameters:
      - type: object
        mapping: $.
    - name: palo-alto-networks-create-browser
      description: Palo Alto Networks Create Browser Deployment
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: palo-alto-prisma-access-browser-deployments.createdeployment
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: palo-alto-networks-get-browser
      description: Palo Alto Networks Get Browser Deployment
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: palo-alto-prisma-access-browser-deployments.getdeployment
      with:
        deployment_id: tools.deployment_id
      outputParameters:
      - type: object
        mapping: $.
    - name: palo-alto-networks-update-browser
      description: Palo Alto Networks Update Browser Deployment
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: palo-alto-prisma-access-browser-deployments.updatedeployment
      with:
        deployment_id: tools.deployment_id
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: palo-alto-networks-delete-browser
      description: Palo Alto Networks Delete Browser Deployment
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: palo-alto-prisma-access-browser-deployments.deletedeployment
      with:
        deployment_id: tools.deployment_id
      outputParameters:
      - type: object
        mapping: $.
