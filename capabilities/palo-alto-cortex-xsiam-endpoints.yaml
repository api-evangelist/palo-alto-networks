naftiko: 1.0.0-alpha2
info:
  label: Palo Alto Networks Cortex XSIAM REST API — Endpoints
  description: 'Palo Alto Networks Cortex XSIAM REST API — Endpoints. 1 operations. Lead operation: Palo Alto Networks List
    Endpoints. Self-contained Naftiko capability covering one Palo Alto Networks business surface.'
  tags:
  - Palo Alto Networks
  - Endpoints
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PALO_ALTO_NETWORKS_API_KEY: PALO_ALTO_NETWORKS_API_KEY
capability:
  consumes:
  - type: http
    namespace: palo-alto-cortex-xsiam-endpoints
    baseUri: https://api-{fqdn}/public_api/v1
    description: Palo Alto Networks Cortex XSIAM REST API — Endpoints business capability. Self-contained, no shared references.
    resources:
    - name: endpoints-get_endpoints
      path: /endpoints/get_endpoints
      operations:
      - name: getendpoints
        method: POST
        description: Palo Alto Networks List Endpoints
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: apikey
      key: x-xdr-hmac-v2
      value: '{{env.PALO_ALTO_NETWORKS_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: palo-alto-cortex-xsiam-endpoints-rest
    port: 8080
    description: REST adapter for Palo Alto Networks Cortex XSIAM REST API — Endpoints. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/endpoints/get-endpoints
      name: endpoints-get-endpoints
      description: REST surface for endpoints-get_endpoints.
      operations:
      - method: POST
        name: getendpoints
        description: Palo Alto Networks List Endpoints
        call: palo-alto-cortex-xsiam-endpoints.getendpoints
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: palo-alto-cortex-xsiam-endpoints-mcp
    port: 9090
    transport: http
    description: MCP adapter for Palo Alto Networks Cortex XSIAM REST API — Endpoints. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: palo-alto-networks-list-endpoints
      description: Palo Alto Networks List Endpoints
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: palo-alto-cortex-xsiam-endpoints.getendpoints
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
