naftiko: 1.0.0-alpha2
info:
  label: Palo Alto Networks Prisma Cloud CSPM API — Reports
  description: 'Palo Alto Networks Prisma Cloud CSPM API — Reports. 1 operations. Lead operation: Palo Alto Networks List
    Reports. Self-contained Naftiko capability covering one Palo Alto Networks business surface.'
  tags:
  - Palo Alto Networks
  - Reports
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PALO_ALTO_NETWORKS_API_KEY: PALO_ALTO_NETWORKS_API_KEY
capability:
  consumes:
  - type: http
    namespace: palo-alto-prisma-cloud-cspm-reports
    baseUri: https://api.prismacloud.io
    description: Palo Alto Networks Prisma Cloud CSPM API — Reports business capability. Self-contained, no shared references.
    resources:
    - name: report
      path: /report
      operations:
      - name: listreports
        method: GET
        description: Palo Alto Networks List Reports
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.PALO_ALTO_NETWORKS_API_KEY}}'
  exposes:
  - type: rest
    namespace: palo-alto-prisma-cloud-cspm-reports-rest
    port: 8080
    description: REST adapter for Palo Alto Networks Prisma Cloud CSPM API — Reports. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/report
      name: report
      description: REST surface for report.
      operations:
      - method: GET
        name: listreports
        description: Palo Alto Networks List Reports
        call: palo-alto-prisma-cloud-cspm-reports.listreports
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: palo-alto-prisma-cloud-cspm-reports-mcp
    port: 9090
    transport: http
    description: MCP adapter for Palo Alto Networks Prisma Cloud CSPM API — Reports. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: palo-alto-networks-list-reports
      description: Palo Alto Networks List Reports
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: palo-alto-prisma-cloud-cspm-reports.listreports
      outputParameters:
      - type: object
        mapping: $.
