naftiko: 1.0.0-alpha2
info:
  label: Palo Alto Networks SASE Configuration Orchestration API — Onboarding
  description: 'Palo Alto Networks SASE Configuration Orchestration API — Onboarding. 1 operations. Lead operation: Palo Alto
    Networks Get Remote Network Onboarding Status. Self-contained Naftiko capability covering one Palo Alto Networks business
    surface.'
  tags:
  - Palo Alto Networks
  - Onboarding
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PALO_ALTO_NETWORKS_API_KEY: PALO_ALTO_NETWORKS_API_KEY
capability:
  consumes:
  - type: http
    namespace: palo-alto-sase-config-orchestration-onboarding
    baseUri: https://api.sase.paloaltonetworks.com/config-orchestration/v1
    description: Palo Alto Networks SASE Configuration Orchestration API — Onboarding business capability. Self-contained,
      no shared references.
    resources:
    - name: onboarding-status-id
      path: /onboarding-status/{id}
      operations:
      - name: getonboardingstatus
        method: GET
        description: Palo Alto Networks Get Remote Network Onboarding Status
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: id
          in: path
          type: string
          description: Unique identifier of the remote network.
          required: true
    authentication:
      type: bearer
      token: '{{env.PALO_ALTO_NETWORKS_API_KEY}}'
  exposes:
  - type: rest
    namespace: palo-alto-sase-config-orchestration-onboarding-rest
    port: 8080
    description: REST adapter for Palo Alto Networks SASE Configuration Orchestration API — Onboarding. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/onboarding-status/{id}
      name: onboarding-status-id
      description: REST surface for onboarding-status-id.
      operations:
      - method: GET
        name: getonboardingstatus
        description: Palo Alto Networks Get Remote Network Onboarding Status
        call: palo-alto-sase-config-orchestration-onboarding.getonboardingstatus
        with:
          id: rest.id
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: palo-alto-sase-config-orchestration-onboarding-mcp
    port: 9090
    transport: http
    description: MCP adapter for Palo Alto Networks SASE Configuration Orchestration API — Onboarding. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: palo-alto-networks-get-remote
      description: Palo Alto Networks Get Remote Network Onboarding Status
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: palo-alto-sase-config-orchestration-onboarding.getonboardingstatus
      with:
        id: tools.id
      outputParameters:
      - type: object
        mapping: $.
