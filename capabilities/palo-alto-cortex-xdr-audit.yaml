naftiko: 1.0.0-alpha2
info:
  label: Palo Alto Networks Cortex XDR REST API — Audit
  description: 'Palo Alto Networks Cortex XDR REST API — Audit. 1 operations. Lead operation: Palo Alto Networks Get Audit
    Management Logs. Self-contained Naftiko capability covering one Palo Alto Networks business surface.'
  tags:
  - Palo Alto Networks
  - Audit
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PALO_ALTO_NETWORKS_API_KEY: PALO_ALTO_NETWORKS_API_KEY
capability:
  consumes:
  - type: http
    namespace: palo-alto-cortex-xdr-audit
    baseUri: https://api-{fqdn}/public_api/v1
    description: Palo Alto Networks Cortex XDR REST API — Audit business capability. Self-contained, no shared references.
    resources:
    - name: audit-get_audit_management_logs
      path: /audit/get_audit_management_logs
      operations:
      - name: getauditmanagementlogs
        method: POST
        description: Palo Alto Networks Get Audit Management Logs
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: apikey
      key: x-xdr-hmac-v2
      value: '{{env.PALO_ALTO_NETWORKS_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: palo-alto-cortex-xdr-audit-rest
    port: 8080
    description: REST adapter for Palo Alto Networks Cortex XDR REST API — Audit. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/audit/get-audit-management-logs
      name: audit-get-audit-management-logs
      description: REST surface for audit-get_audit_management_logs.
      operations:
      - method: POST
        name: getauditmanagementlogs
        description: Palo Alto Networks Get Audit Management Logs
        call: palo-alto-cortex-xdr-audit.getauditmanagementlogs
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: palo-alto-cortex-xdr-audit-mcp
    port: 9090
    transport: http
    description: MCP adapter for Palo Alto Networks Cortex XDR REST API — Audit. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: palo-alto-networks-get-audit
      description: Palo Alto Networks Get Audit Management Logs
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: palo-alto-cortex-xdr-audit.getauditmanagementlogs
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
