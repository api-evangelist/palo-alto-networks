naftiko: 1.0.0-alpha2
info:
  label: Palo Alto Networks Security Advisory API — Products
  description: 'Palo Alto Networks Security Advisory API — Products. 1 operations. Lead operation: Palo Alto Networks List
    Affected Products. Self-contained Naftiko capability covering one Palo Alto Networks business surface.'
  tags:
  - Palo Alto Networks
  - Products
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PALO_ALTO_NETWORKS_API_KEY: PALO_ALTO_NETWORKS_API_KEY
capability:
  consumes:
  - type: http
    namespace: palo-alto-security-advisory-products
    baseUri: https://security.paloaltonetworks.com/api
    description: Palo Alto Networks Security Advisory API — Products business capability. Self-contained, no shared references.
    resources:
    - name: products
      path: /products
      operations:
      - name: listaffectedproducts
        method: GET
        description: Palo Alto Networks List Affected Products
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: palo-alto-security-advisory-products-rest
    port: 8080
    description: REST adapter for Palo Alto Networks Security Advisory API — Products. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/products
      name: products
      description: REST surface for products.
      operations:
      - method: GET
        name: listaffectedproducts
        description: Palo Alto Networks List Affected Products
        call: palo-alto-security-advisory-products.listaffectedproducts
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: palo-alto-security-advisory-products-mcp
    port: 9090
    transport: http
    description: MCP adapter for Palo Alto Networks Security Advisory API — Products. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: palo-alto-networks-list-affected
      description: Palo Alto Networks List Affected Products
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: palo-alto-security-advisory-products.listaffectedproducts
      outputParameters:
      - type: object
        mapping: $.
