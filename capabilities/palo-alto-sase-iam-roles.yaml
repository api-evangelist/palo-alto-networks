naftiko: 1.0.0-alpha2
info:
  label: Palo Alto Networks SASE IAM Service API — Roles
  description: 'Palo Alto Networks SASE IAM Service API — Roles. 1 operations. Lead operation: Palo Alto Networks List Available
    Roles. Self-contained Naftiko capability covering one Palo Alto Networks business surface.'
  tags:
  - Palo Alto Networks
  - Roles
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PALO_ALTO_NETWORKS_API_KEY: PALO_ALTO_NETWORKS_API_KEY
capability:
  consumes:
  - type: http
    namespace: palo-alto-sase-iam-roles
    baseUri: https://api.sase.paloaltonetworks.com/iam/v1
    description: Palo Alto Networks SASE IAM Service API — Roles business capability. Self-contained, no shared references.
    resources:
    - name: roles
      path: /roles
      operations:
      - name: listroles
        method: GET
        description: Palo Alto Networks List Available Roles
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.PALO_ALTO_NETWORKS_API_KEY}}'
  exposes:
  - type: rest
    namespace: palo-alto-sase-iam-roles-rest
    port: 8080
    description: REST adapter for Palo Alto Networks SASE IAM Service API — Roles. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/roles
      name: roles
      description: REST surface for roles.
      operations:
      - method: GET
        name: listroles
        description: Palo Alto Networks List Available Roles
        call: palo-alto-sase-iam-roles.listroles
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: palo-alto-sase-iam-roles-mcp
    port: 9090
    transport: http
    description: MCP adapter for Palo Alto Networks SASE IAM Service API — Roles. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: palo-alto-networks-list-available
      description: Palo Alto Networks List Available Roles
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: palo-alto-sase-iam-roles.listroles
      outputParameters:
      - type: object
        mapping: $.
