openapi: 3.2.0
info:
  title: Palo Alto Networks Defenders API
  version: '1.0'
  description: 'Operations tagged Defenders across 4 of this provider''s published API definitions: palo-alto-compute-34-03-openapi-34-03-138-sh-openapi.json, palo-alto-compute-openapi-34-04-145-sh-openapi.json, palo-alto-cwpp-34-03-openapi-34-03-138-saas-openapi.json, palo-alto-cwpp-openapi-34-04-145-saas-openapi.json. Each path carries the servers of the definition it was published in.'
servers:
- url: PATH_TO_CONSOLE
tags:
- name: Defenders
  description: Defenders. 21 operations in this definition.
  x-description-source: desc/defenders/defenders.md
paths:
  /api/v34.03/defenders:
    get:
      description: 'Get Deployed Defenders. GET /api/v34.03/defenders on the Defenders API. Takes 18 query parameters. Documented responses: 200.'
      parameters:
      - description: 'Offsets the result to a specific report count. Offset starts from 0.

          '
        in: query
        name: offset
        schema:
          type: integer
      - description: 'Limit is the amount to fix.

          '
        in: query
        name: limit
        schema:
          type: integer
      - description: 'Sorts the result using a key.

          '
        in: query
        name: sort
        schema:
          type: string
      - description: 'Sorts the result in reverse order.

          '
        in: query
        name: reverse
        schema:
          type: boolean
      - description: 'Hostname is a name of a specific Defender to retrieve.

          '
        in: query
        name: hostname
        schema:
          type: string
      - description: 'Roles are the defender api.Roles to filter.

          '
        in: query
        name: role
        schema:
          description: 'Roles are the defender api.Roles to filter.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Indicates whether to return only connected Defenders (true) or disconnected Defenders (false).

          '
        in: query
        name: connected
        schema:
          type: boolean
      - description: 'Indicates the Defender types to return (e.g., docker, dockerWindows, cri, etc).

          '
        in: query
        name: type
        schema:
          description: 'Indicates the Defender types to return (e.g., docker, dockerWindows, cri, etc).

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Indicates whether to return a list of Defenders that are running the latest version of Prisma Cloud (true)

          or defenders with older versions (false).

          '
        in: query
        name: latest
        schema:
          type: boolean
      - description: 'SupportedVersion indicates only Defenders of supported versions should be fetched.

          '
        in: query
        name: supportedVersion
        schema:
          type: boolean
      - description: 'Scopes the query by cluster name.

          '
        in: query
        name: cluster
        schema:
          description: 'Scopes the query by cluster name.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Scopes the query by TAS cluster IDs.

          '
        in: query
        name: tasClusterIDs
        schema:
          description: 'Scopes the query by TAS cluster IDs.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Scopes the query by TAS blobstore scanning only Defenders (true) or TAS full coverage Defenders (false).

          '
        in: query
        name: tasBlobstoreScanner
        schema:
          type: boolean
      - description: 'Scopes the query by TAS foundations.

          '
        in: query
        name: tasFoundations
        schema:
          description: 'Scopes the query by TAS foundations.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Scopes the query to defenders which are using old certificate.

          '
        in: query
        name: usingOldCA
        schema:
          type: boolean
      - description: 'Scopes the query to defenders which are using expired certificate.

          '
        in: query
        name: usingExpiredCA
        schema:
          type: boolean
      - description: 'Scopes the query by provider type

          Indicates whether to return only defenders running on ARM64 architecture.

          '
        in: query
        name: isARM64
        schema:
          type: boolean
      - description: 'Indicates whether to return only defenders running as VPC Observer.

          '
        in: query
        name: isVPCObserver
        schema:
          type: boolean
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/-_defender.Defender'
          description: ''
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: get-defenders
      summary: Get Deployed Defenders
      x-description-source: desc/defenders/get.md
  /api/v34.03/defenders/aci.yaml:
    post:
      description: 'ACIContainerGroupYAML receives ACI Container group definition in YAML format, augments the yaml to include defenders, and return a new YAML

        '
      parameters:
      - description: 'ConsoleAddr is the remote console address.

          '
        in: query
        name: consoleaddr
        schema:
          type: string
      - description: 'Interpreter is a custom interpreter set by the user to run the defender entrypoint script.

          '
        in: query
        name: interpreter
        schema:
          type: string
      - description: 'FilesystemMonitoring is the filesystem monitoring flag.

          '
        in: query
        name: filesystemMonitoring
        schema:
          type: boolean
      - description: 'DefenderImage is the full path to the Defender image, if not specified Prisma''s private registry is used.

          '
        in: query
        name: defenderImage
        schema:
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/-_uint8'
          description: ''
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: post-defenders-aci.yaml
      summary: ' A CI Container Group Y A M L'
  /api/v34.03/defenders/app-embedded:
    post:
      description: 'Generate a Docker File for App-embedded Defender. POST /api/v34.03/defenders/app-embedded on the Defenders API. Takes an optional request body. Documented responses: 200.'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/shared.AppEmbeddedEmbedRequest'
      responses:
        '200':
          description: OK
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: post-defenders-app-embedded
      summary: Generate a Docker File for App-embedded Defender
      x-description-source: desc/defenders/app_embedded_post.md
  /api/v34.03/defenders/cloud-run.yaml:
    post:
      description: 'CloudRunServiceYAML receives Cloud run Service in YAML format, augments the yaml to include defenders, and return a new YAML

        '
      parameters:
      - description: 'ConsoleAddr is the remote console address.

          '
        in: query
        name: consoleaddr
        schema:
          type: string
      - description: 'Interpreter is a custom interpreter set by the user to run the defender entrypoint script.

          '
        in: query
        name: interpreter
        schema:
          type: string
      - description: 'FilesystemMonitoring is the filesystem monitoring flag.

          '
        in: query
        name: filesystemMonitoring
        schema:
          type: boolean
      - description: 'DefenderImage is the full path to the Defender image, if not specified Prisma''s private registry is used.

          '
        in: query
        name: defenderImage
        schema:
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/-_uint8'
          description: ''
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: post-defenders-cloud-run.yaml
      summary: ' Cloud Run Service Y A M L'
  /api/v34.03/defenders/daemonset.yaml:
    post:
      description: 'Generate Daemonset Deployment YAML File. POST /api/v34.03/defenders/daemonset.yaml on the Defenders API. Takes an optional request body. Documented responses: 200.'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/common.DaemonSetOptions'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/-_uint8'
          description: ''
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: post-defenders-daemonset.yaml
      summary: Generate Daemonset Deployment YAML File
      x-description-source: desc/defenders/daemonset_yaml_post.md
  /api/v34.03/defenders/download:
    get:
      description: 'Download Deployed Defenders. GET /api/v34.03/defenders/download on the Defenders API. Takes 18 query parameters. Documented responses: 200.'
      parameters:
      - description: 'Offsets the result to a specific report count. Offset starts from 0.

          '
        in: query
        name: offset
        schema:
          type: integer
      - description: 'Limit is the amount to fix.

          '
        in: query
        name: limit
        schema:
          type: integer
      - description: 'Sorts the result using a key.

          '
        in: query
        name: sort
        schema:
          type: string
      - description: 'Sorts the result in reverse order.

          '
        in: query
        name: reverse
        schema:
          type: boolean
      - description: 'Hostname is a name of a specific Defender to retrieve.

          '
        in: query
        name: hostname
        schema:
          type: string
      - description: 'Roles are the defender api.Roles to filter.

          '
        in: query
        name: role
        schema:
          description: 'Roles are the defender api.Roles to filter.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Indicates whether to return only connected Defenders (true) or disconnected Defenders (false).

          '
        in: query
        name: connected
        schema:
          type: boolean
      - description: 'Indicates the Defender types to return (e.g., docker, dockerWindows, cri, etc).

          '
        in: query
        name: type
        schema:
          description: 'Indicates the Defender types to return (e.g., docker, dockerWindows, cri, etc).

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Indicates whether to return a list of Defenders that are running the latest version of Prisma Cloud (true)

          or defenders with older versions (false).

          '
        in: query
        name: latest
        schema:
          type: boolean
      - description: 'SupportedVersion indicates only Defenders of supported versions should be fetched.

          '
        in: query
        name: supportedVersion
        schema:
          type: boolean
      - description: 'Scopes the query by cluster name.

          '
        in: query
        name: cluster
        schema:
          description: 'Scopes the query by cluster name.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Scopes the query by TAS cluster IDs.

          '
        in: query
        name: tasClusterIDs
        schema:
          description: 'Scopes the query by TAS cluster IDs.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Scopes the query by TAS blobstore scanning only Defenders (true) or TAS full coverage Defenders (false).

          '
        in: query
        name: tasBlobstoreScanner
        schema:
          type: boolean
      - description: 'Scopes the query by TAS foundations.

          '
        in: query
        name: tasFoundations
        schema:
          description: 'Scopes the query by TAS foundations.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Scopes the query to defenders which are using old certificate.

          '
        in: query
        name: usingOldCA
        schema:
          type: boolean
      - description: 'Scopes the query to defenders which are using expired certificate.

          '
        in: query
        name: usingExpiredCA
        schema:
          type: boolean
      - description: 'Scopes the query by provider type

          Indicates whether to return only defenders running on ARM64 architecture.

          '
        in: query
        name: isARM64
        schema:
          type: boolean
      - description: 'Indicates whether to return only defenders running as VPC Observer.

          '
        in: query
        name: isVPCObserver
        schema:
          type: boolean
      responses:
        '200':
          description: OK
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: get-defenders-download
      summary: Download Deployed Defenders
      x-description-source: desc/defenders/download_get.md
  /api/v34.03/defenders/eks-fargate.yaml:
    post:
      description: 'GenerateK8sControllerManifest receives a Kubernetes controller definition in YAML format,

        augments the YAML to include defenders, and returns a new YAML.

        '
      parameters:
      - description: 'ConsoleAddr is the remote console address.

          '
        in: query
        name: consoleaddr
        schema:
          type: string
      - description: 'Interpreter is a custom interpreter set by the user to run the defender entrypoint script.

          '
        in: query
        name: interpreter
        schema:
          type: string
      - description: 'FilesystemMonitoring is the filesystem monitoring flag.

          '
        in: query
        name: filesystemMonitoring
        schema:
          type: boolean
      - description: 'DefenderImage is the full path to the Defender image, if not specified Prisma''s private registry is used.

          '
        in: query
        name: defenderImage
        schema:
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/-_uint8'
          description: ''
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: post-defenders-eks-fargate.yaml
      summary: ' Generate K8s Controller Manifest'
  /api/v34.03/defenders/fargate.json:
    post:
      description: 'Generate a Protected JSON Fargate Task Definition. POST /api/v34.03/defenders/fargate.json on the Defenders API. Takes 10 query parameters, an optional request body. Documented responses: 200.'
      parameters:
      - description: 'ConsoleAddr is the remote console address.

          '
        in: query
        name: consoleaddr
        schema:
          type: string
      - description: 'DefenderType is the type of the defender to create the install bundle for.

          '
        in: query
        name: defenderType
        schema:
          description: Type is the type to be given at startup
          enum:
          - - none
            - docker
            - dockerWindows
            - containerdWindows
            - swarm
            - daemonset
            - serverLinux
            - serverWindows
            - cri
            - fargate
            - appEmbedded
            - tas
            - tasWindows
            - serverless
            - ecs
            - podman
            - eksFargate
          type: string
      - description: 'Interpreter is a custom interpreter set by the user to run the fargate defender entrypoint script.

          '
        in: query
        name: interpreter
        schema:
          type: string
      - description: 'CloudFormation indicates if the given fargate task definition is in Cloud Formation format.

          '
        in: query
        name: cloudFormation
        schema:
          type: boolean
      - description: 'FilesystemMonitoring is the filesystem monitoring flag.

          '
        in: query
        name: filesystemMonitoring
        schema:
          type: boolean
      - description: 'ExtractEntrypoint indicates if entrypoint will be extracted automatically.

          '
        in: query
        name: extractEntrypoint
        schema:
          type: boolean
      - description: 'RegistryType is the registry type for fetching image details needed to create fargate task definition (e.g., dockerhub).

          '
        in: query
        name: registryType
        schema:
          type: string
      - description: 'RegistryCredentialID of the credentials in the credentials store to use for authenticating with the registry.

          '
        in: query
        name: registryCredentialID
        schema:
          type: string
      - description: 'DefenderImage is the full path to the Defender image, if not specified Prisma''s private registry is used.

          '
        in: query
        name: defenderImage
        schema:
          type: string
      - description: 'DefenderImagePullSecret is the name of the secret required to pull the Defender image from private registry.

          '
        in: query
        name: defenderImagePullSecret
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/appembedded.FargateTask'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/appembedded.FargateTask'
          description: FargateTask represents the generic fargate task AWS template
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: post-defenders-fargate.json
      summary: Generate a Protected JSON Fargate Task Definition
      x-description-source: desc/defenders/fargate_json_post.md
  /api/v34.03/defenders/fargate.yaml:
    post:
      description: 'Generate a Protected YAML Fargate Task Definition. POST /api/v34.03/defenders/fargate.yaml on the Defenders API. Takes 10 query parameters. Documented responses: 200.'
      parameters:
      - description: 'ConsoleAddr is the remote console address.

          '
        in: query
        name: consoleaddr
        schema:
          type: string
      - description: 'DefenderType is the type of the defender to create the install bundle for.

          '
        in: query
        name: defenderType
        schema:
          description: Type is the type to be given at startup
          enum:
          - - none
            - docker
            - dockerWindows
            - containerdWindows
            - swarm
            - daemonset
            - serverLinux
            - serverWindows
            - cri
            - fargate
            - appEmbedded
            - tas
            - tasWindows
            - serverless
            - ecs
            - podman
            - eksFargate
          type: string
      - description: 'Interpreter is a custom interpreter set by the user to run the fargate defender entrypoint script.

          '
        in: query
        name: interpreter
        schema:
          type: string
      - description: 'CloudFormation indicates if the given fargate task definition is in Cloud Formation format.

          '
        in: query
        name: cloudFormation
        schema:
          type: boolean
      - description: 'FilesystemMonitoring is the filesystem monitoring flag.

          '
        in: query
        name: filesystemMonitoring
        schema:
          type: boolean
      - description: 'ExtractEntrypoint indicates if entrypoint will be extracted automatically.

          '
        in: query
        name: extractEntrypoint
        schema:
          type: boolean
      - description: 'RegistryType is the registry type for fetching image details needed to create fargate task definition (e.g., dockerhub).

          '
        in: query
        name: registryType
        schema:
          type: string
      - description: 'RegistryCredentialID of the credentials in the credentials store to use for authenticating with the registry.

          '
        in: query
        name: registryCredentialID
        schema:
          type: string
      - description: 'DefenderImage is the full path to the Defender image, if not specified Prisma''s private registry is used.

          '
        in: query
        name: defenderImage
        schema:
          type: string
      - description: 'DefenderImagePullSecret is the name of the secret required to pull the Defender image from private registry.

          '
        in: query
        name: defenderImagePullSecret
        schema:
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/-_uint8'
          description: ''
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: post-defenders-fargate.yaml
      summary: Generate a Protected YAML Fargate Task Definition
      x-description-source: desc/defenders/fargate_yaml_post.md
  /api/v34.03/defenders/helm/twistlock-defender-helm.tar.gz:
    post:
      description: 'Generate a Helm Deployment Chart for Defender. POST /api/v34.03/defenders/helm/twistlock-defender-helm.tar.gz on the Defenders API. Takes an optional request body. Documented responses: 200.'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/common.DaemonSetOptions'
      responses:
        '200':
          description: OK
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: post-defenders-helm-twistlock-defender-helm.tar.gz
      summary: Generate a Helm Deployment Chart for Defender
      x-description-source: desc/defenders/helm_post.md
  /api/v34.03/defenders/image-name:
    get:
      description: 'Get Docker Image Name for Defender. GET /api/v34.03/defenders/image-name on the Defenders API. Documented responses: 200.'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/string'
          description: ''
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: get-defenders-image-name
      summary: Get Docker Image Name for Defender
      x-description-source: desc/defenders/image-name_get.md
  /api/v34.03/defenders/install-bundle:
    get:
      description: 'Get Certificate Bundle for Defender. GET /api/v34.03/defenders/install-bundle on the Defenders API. Takes 10 query parameters. Documented responses: 200.'
      parameters:
      - description: 'ConsoleAddr is the remote console address.

          '
        in: query
        name: consoleaddr
        schema:
          type: string
      - description: 'DefenderType is the type of the defender to create the install bundle for.

          '
        in: query
        name: defenderType
        schema:
          description: Type is the type to be given at startup
          enum:
          - - none
            - docker
            - dockerWindows
            - containerdWindows
            - swarm
            - daemonset
            - serverLinux
            - serverWindows
            - cri
            - fargate
            - appEmbedded
            - tas
            - tasWindows
            - serverless
            - ecs
            - podman
            - eksFargate
          type: string
      - description: 'Interpreter is a custom interpreter set by the user to run the fargate defender entrypoint script.

          '
        in: query
        name: interpreter
        schema:
          type: string
      - description: 'CloudFormation indicates if the given fargate task definition is in Cloud Formation format.

          '
        in: query
        name: cloudFormation
        schema:
          type: boolean
      - description: 'FilesystemMonitoring is the filesystem monitoring flag.

          '
        in: query
        name: filesystemMonitoring
        schema:
          type: boolean
      - description: 'ExtractEntrypoint indicates if entrypoint will be extracted automatically.

          '
        in: query
        name: extractEntrypoint
        schema:
          type: boolean
      - description: 'RegistryType is the registry type for fetching image details needed to create fargate task definition (e.g., dockerhub).

          '
        in: query
        name: registryType
        schema:
          type: string
      - description: 'RegistryCredentialID of the credentials in the credentials store to use for authenticating with the registry.

          '
        in: query
        name: registryCredentialID
        schema:
          type: string
      - description: 'DefenderImage is the full path to the Defender image, if not specified Prisma''s private registry is used.

          '
        in: query
        name: defenderImage
        schema:
          type: string
      - description: 'DefenderImagePullSecret is the name of the secret required to pull the Defender image from private registry.

          '
        in: query
        name: defenderImagePullSecret
        schema:
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/shared.DefenderInstallBundle'
          description: DefenderInstallBundle represents the install bundle for the defender
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: get-defenders-install-bundle
      summary: Get Certificate Bundle for Defender
      x-description-source: desc/defenders/install-bundle_get.md
  /api/v34.03/defenders/names:
    get:
      description: 'Get Defender Names. GET /api/v34.03/defenders/names on the Defenders API. Takes 18 query parameters. Documented responses: 200.'
      parameters:
      - description: 'Offsets the result to a specific report count. Offset starts from 0.

          '
        in: query
        name: offset
        schema:
          type: integer
      - description: 'Limit is the amount to fix.

          '
        in: query
        name: limit
        schema:
          type: integer
      - description: 'Sorts the result using a key.

          '
        in: query
        name: sort
        schema:
          type: string
      - description: 'Sorts the result in reverse order.

          '
        in: query
        name: reverse
        schema:
          type: boolean
      - description: 'Hostname is a name of a specific Defender to retrieve.

          '
        in: query
        name: hostname
        schema:
          type: string
      - description: 'Roles are the defender api.Roles to filter.

          '
        in: query
        name: role
        schema:
          description: 'Roles are the defender api.Roles to filter.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Indicates whether to return only connected Defenders (true) or disconnected Defenders (false).

          '
        in: query
        name: connected
        schema:
          type: boolean
      - description: 'Indicates the Defender types to return (e.g., docker, dockerWindows, cri, etc).

          '
        in: query
        name: type
        schema:
          description: 'Indicates the Defender types to return (e.g., docker, dockerWindows, cri, etc).

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Indicates whether to return a list of Defenders that are running the latest version of Prisma Cloud (true)

          or defenders with older versions (false).

          '
        in: query
        name: latest
        schema:
          type: boolean
      - description: 'SupportedVersion indicates only Defenders of supported versions should be fetched.

          '
        in: query
        name: supportedVersion
        schema:
          type: boolean
      - description: 'Scopes the query by cluster name.

          '
        in: query
        name: cluster
        schema:
          description: 'Scopes the query by cluster name.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Scopes the query by TAS cluster IDs.

          '
        in: query
        name: tasClusterIDs
        schema:
          description: 'Scopes the query by TAS cluster IDs.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Scopes the query by TAS blobstore scanning only Defenders (true) or TAS full coverage Defenders (false).

          '
        in: query
        name: tasBlobstoreScanner
        schema:
          type: boolean
      - description: 'Scopes the query by TAS foundations.

          '
        in: query
        name: tasFoundations
        schema:
          description: 'Scopes the query by TAS foundations.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Scopes the query to defenders which are using old certificate.

          '
        in: query
        name: usingOldCA
        schema:
          type: boolean
      - description: 'Scopes the query to defenders which are using expired certificate.

          '
        in: query
        name: usingExpiredCA
        schema:
          type: boolean
      - description: 'Scopes the query by provider type

          Indicates whether to return only defenders running on ARM64 architecture.

          '
        in: query
        name: isARM64
        schema:
          type: boolean
      - description: 'Indicates whether to return only defenders running as VPC Observer.

          '
        in: query
        name: isVPCObserver
        schema:
          type: boolean
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/-_string'
          description: ''
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: get-defenders-names
      summary: Get Defender Names
      x-description-source: desc/defenders/names_get.md
  /api/v34.03/defenders/serverless/bundle:
    post:
      description: 'Generate Serverless Bundle for Defender. POST /api/v34.03/defenders/serverless/bundle on the Defenders API. Takes an optional request body. Documented responses: 200.'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/shared.ServerlessBundleRequest'
      responses:
        '200':
          description: OK
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: post-defenders-serverless-bundle
      summary: Generate Serverless Bundle for Defender
      x-description-source: desc/defenders/serverless-bundle_post.md
  /api/v34.03/defenders/summary:
    get:
      description: 'Get Defenders Summary. GET /api/v34.03/defenders/summary on the Defenders API. Documented responses: 200.'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/-_types.DefenderSummary'
          description: ''
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: get-defenders-summary
      summary: Get Defenders Summary
      x-description-source: desc/defenders/summary_get.md
  /api/v34.03/defenders/tas-cloud-controller-address:
    get:
      description: 'Defenders Tas Cloud Controller Address. GET /api/v34.03/defenders/tas-cloud-controller-address on the Defenders API. Takes 18 query parameters. Documented responses: 200.'
      parameters:
      - description: 'Offsets the result to a specific report count. Offset starts from 0.

          '
        in: query
        name: offset
        schema:
          type: integer
      - description: 'Limit is the amount to fix.

          '
        in: query
        name: limit
        schema:
          type: integer
      - description: 'Sorts the result using a key.

          '
        in: query
        name: sort
        schema:
          type: string
      - description: 'Sorts the result in reverse order.

          '
        in: query
        name: reverse
        schema:
          type: boolean
      - description: 'Hostname is a name of a specific Defender to retrieve.

          '
        in: query
        name: hostname
        schema:
          type: string
      - description: 'Roles are the defender api.Roles to filter.

          '
        in: query
        name: role
        schema:
          description: 'Roles are the defender api.Roles to filter.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Indicates whether to return only connected Defenders (true) or disconnected Defenders (false).

          '
        in: query
        name: connected
        schema:
          type: boolean
      - description: 'Indicates the Defender types to return (e.g., docker, dockerWindows, cri, etc).

          '
        in: query
        name: type
        schema:
          description: 'Indicates the Defender types to return (e.g., docker, dockerWindows, cri, etc).

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Indicates whether to return a list of Defenders that are running the latest version of Prisma Cloud (true)

          or defenders with older versions (false).

          '
        in: query
        name: latest
        schema:
          type: boolean
      - description: 'SupportedVersion indicates only Defenders of supported versions should be fetched.

          '
        in: query
        name: supportedVersion
        schema:
          type: boolean
      - description: 'Scopes the query by cluster name.

          '
        in: query
        name: cluster
        schema:
          description: 'Scopes the query by cluster name.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Scopes the query by TAS cluster IDs.

          '
        in: query
        name: tasClusterIDs
        schema:
          description: 'Scopes the query by TAS cluster IDs.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Scopes the query by TAS blobstore scanning only Defenders (true) or TAS full coverage Defenders (false).

          '
        in: query
        name: tasBlobstoreScanner
        schema:
          type: boolean
      - description: 'Scopes the query by TAS foundations.

          '
        in: query
        name: tasFoundations
        schema:
          description: 'Scopes the query by TAS foundations.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Scopes the query to defenders which are using old certificate.

          '
        in: query
        name: usingOldCA
        schema:
          type: boolean
      - description: 'Scopes the query to defenders which are using expired certificate.

          '
        in: query
        name: usingExpiredCA
        schema:
          type: boolean
      - description: 'Scopes the query by provider type

          Indicates whether to return only defenders running on ARM64 architecture.

          '
        in: query
        name: isARM64
        schema:
          type: boolean
      - description: 'Indicates whether to return only defenders running as VPC Observer.

          '
        in: query
        name: isVPCObserver
        schema:
          type: boolean
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/-_string'
          description: ''
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: get-defenders-tas-cloud-controller-address
      summary: Defenders Tas Cloud Controller Address
      x-description-source: desc/defenders/tas-cloud-controller-address_get.md
  /api/v34.03/defenders/upgrade:
    post:
      description: 'Upgrade Connected Single Linux Defenders. POST /api/v34.03/defenders/upgrade on the Defenders API. Takes 18 query parameters. Documented responses: 200.'
      parameters:
      - description: 'Offsets the result to a specific report count. Offset starts from 0.

          '
        in: query
        name: offset
        schema:
          type: integer
      - description: 'Limit is the amount to fix.

          '
        in: query
        name: limit
        schema:
          type: integer
      - description: 'Sorts the result using a key.

          '
        in: query
        name: sort
        schema:
          type: string
      - description: 'Sorts the result in reverse order.

          '
        in: query
        name: reverse
        schema:
          type: boolean
      - description: 'Hostname is a name of a specific Defender to retrieve.

          '
        in: query
        name: hostname
        schema:
          type: string
      - description: 'Roles are the defender api.Roles to filter.

          '
        in: query
        name: role
        schema:
          description: 'Roles are the defender api.Roles to filter.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Indicates whether to return only connected Defenders (true) or disconnected Defenders (false).

          '
        in: query
        name: connected
        schema:
          type: boolean
      - description: 'Indicates the Defender types to return (e.g., docker, dockerWindows, cri, etc).

          '
        in: query
        name: type
        schema:
          description: 'Indicates the Defender types to return (e.g., docker, dockerWindows, cri, etc).

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Indicates whether to return a list of Defenders that are running the latest version of Prisma Cloud (true)

          or defenders with older versions (false).

          '
        in: query
        name: latest
        schema:
          type: boolean
      - description: 'SupportedVersion indicates only Defenders of supported versions should be fetched.

          '
        in: query
        name: supportedVersion
        schema:
          type: boolean
      - description: 'Scopes the query by cluster name.

          '
        in: query
        name: cluster
        schema:
          description: 'Scopes the query by cluster name.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Scopes the query by TAS cluster IDs.

          '
        in: query
        name: tasClusterIDs
        schema:
          description: 'Scopes the query by TAS cluster IDs.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Scopes the query by TAS blobstore scanning only Defenders (true) or TAS full coverage Defenders (false).

          '
        in: query
        name: tasBlobstoreScanner
        schema:
          type: boolean
      - description: 'Scopes the query by TAS foundations.

          '
        in: query
        name: tasFoundations
        schema:
          description: 'Scopes the query by TAS foundations.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Scopes the query to defenders which are using old certificate.

          '
        in: query
        name: usingOldCA
        schema:
          type: boolean
      - description: 'Scopes the query to defenders which are using expired certificate.

          '
        in: query
        name: usingExpiredCA
        schema:
          type: boolean
      - description: 'Scopes the query by provider type

          Indicates whether to return only defenders running on ARM64 architecture.

          '
        in: query
        name: isARM64
        schema:
          type: boolean
      - description: 'Indicates whether to return only defenders running as VPC Observer.

          '
        in: query
        name: isVPCObserver
        schema:
          type: boolean
      responses:
        '200':
          description: OK
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: post-defenders-upgrade
      summary: Upgrade Connected Single Linux Defenders
      x-description-source: desc/defenders/upgrade_post.md
  /api/v34.03/defenders/{id}:
    delete:
      description: 'Delete a Defender. DELETE /api/v34.03/defenders/{id} on the Defenders API. Takes 1 path parameter. Documented responses: 200.'
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        '200':
          description: OK
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: delete-defenders-id
      summary: Delete a Defender
      x-description-source: desc/defenders/id_delete.md
  /api/v34.03/defenders/{id}/features:
    post:
      description: 'Update Defender Configuration. POST /api/v34.03/defenders/{id}/features on the Defenders API. Takes 1 path parameter, an optional request body. Documented responses: 200.'
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/defender.Features'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/defender.Defender'
          description: Defender is an update about an agent starting
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: post-defenders-id-features
      summary: Update Defender Configuration
      x-description-source: desc/defenders/id_features_post.md
  /api/v34.03/defenders/{id}/restart:
    post:
      description: 'Restart a Defender. POST /api/v34.03/defenders/{id}/restart on the Defenders API. Takes 1 path parameter. Documented responses: 200.'
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        '200':
          description: OK
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: post-defenders-id-restart
      summary: Restart a Defender
      x-description-source: desc/defenders/id_restart_post.md
  /api/v34.03/defenders/{id}/upgrade:
    post:
      description: 'Upgrade a Defender. POST /api/v34.03/defenders/{id}/upgrade on the Defenders API. Takes 1 path parameter. Documented responses: 200.'
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        '200':
          description: OK
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: post-defenders-id-upgrade
      summary: Upgrade a Defender
      x-description-source: desc/defenders/id_upgrade_post.md
  /api/v34.04/defenders:
    get:
      description: 'Get Deployed Defenders. GET /api/v34.04/defenders on the Defenders API. Takes 18 query parameters. Documented responses: 200.'
      parameters:
      - description: 'Offsets the result to a specific report count. Offset starts from 0.

          '
        in: query
        name: offset
        schema:
          type: integer
      - description: 'Limit is the amount to fix.

          '
        in: query
        name: limit
        schema:
          type: integer
      - description: 'Sorts the result using a key.

          '
        in: query
        name: sort
        schema:
          type: string
      - description: 'Sorts the result in reverse order.

          '
        in: query
        name: reverse
        schema:
          type: boolean
      - description: 'Hostname is a name of a specific Defender to retrieve.

          '
        in: query
        name: hostname
        schema:
          type: string
      - description: 'Roles are the defender api.Roles to filter.

          '
        in: query
        name: role
        schema:
          description: 'Roles are the defender api.Roles to filter.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Indicates whether to return only connected Defenders (true) or disconnected Defenders (false).

          '
        in: query
        name: connected
        schema:
          type: boolean
      - description: 'Indicates the Defender types to return (e.g., docker, dockerWindows, cri, etc).

          '
        in: query
        name: type
        schema:
          description: 'Indicates the Defender types to return (e.g., docker, dockerWindows, cri, etc).

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Indicates whether to return a list of Defenders that are running the latest version of Prisma Cloud (true)

          or defenders with older versions (false).

          '
        in: query
        name: latest
        schema:
          type: boolean
      - description: 'SupportedVersion indicates only Defenders of supported versions should be fetched.

          '
        in: query
        name: supportedVersion
        schema:
          type: boolean
      - description: 'Scopes the query by cluster name.

          '
        in: query
        name: cluster
        schema:
          description: 'Scopes the query by cluster name.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Scopes the query by TAS cluster IDs.

          '
        in: query
        name: tasClusterIDs
        schema:
          description: 'Scopes the query by TAS cluster IDs.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Scopes the query by TAS blobstore scanning only Defenders (true) or TAS full coverage Defenders (false).

          '
        in: query
        name: tasBlobstoreScanner
        schema:
          type: boolean
      - description: 'Scopes the query by TAS foundations.

          '
        in: query
        name: tasFoundations
        schema:
          description: 'Scopes the query by TAS foundations.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Scopes the query to defenders which are using old certificate.

          '
        in: query
        name: usingOldCA
        schema:
          type: boolean
      - description: 'Scopes the query to defenders which are using expired certificate.

          '
        in: query
        name: usingExpiredCA
        schema:
          type: boolean
      - description: 'Scopes the query by provider type

          Indicates whether to return only defenders running on ARM64 architecture.

          '
        in: query
        name: isARM64
        schema:
          type: boolean
      - description: 'Indicates whether to return only defenders running as VPC Observer.

          '
        in: query
        name: isVPCObserver
        schema:
          type: boolean
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/-_defender.Defender'
          description: ''
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: get-defenders
      summary: Get Deployed Defenders
      x-description-source: desc/defenders/get.md
  /api/v34.04/defenders/aci.yaml:
    post:
      description: 'ACIContainerGroupYAML receives ACI Container group definition in YAML format, augments the yaml to include defenders, and return a new YAML

        '
      parameters:
      - description: 'ConsoleAddr is the remote console address.

          '
        in: query
        name: consoleaddr
        schema:
          type: string
      - description: 'Interpreter is a custom interpreter set by the user to run the defender entrypoint script.

          '
        in: query
        name: interpreter
        schema:
          type: string
      - description: 'FilesystemMonitoring is the filesystem monitoring flag.

          '
        in: query
        name: filesystemMonitoring
        schema:
          type: boolean
      - description: 'DefenderImage is the full path to the Defender image, if not specified Prisma''s private registry is used.

          '
        in: query
        name: defenderImage
        schema:
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/-_uint8'
          description: ''
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: post-defenders-aci.yaml
      summary: ' A CI Container Group Y A M L'
  /api/v34.04/defenders/app-embedded:
    post:
      description: 'Generate a Docker File for App-embedded Defender. POST /api/v34.04/defenders/app-embedded on the Defenders API. Takes an optional request body. Documented responses: 200.'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/shared.AppEmbeddedEmbedRequest'
      responses:
        '200':
          description: OK
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: post-defenders-app-embedded
      summary: Generate a Docker File for App-embedded Defender
      x-description-source: desc/defenders/app_embedded_post.md
  /api/v34.04/defenders/cloud-run.yaml:
    post:
      description: 'CloudRunServiceYAML receives Cloud run Service in YAML format, augments the yaml to include defenders, and return a new YAML

        '
      parameters:
      - description: 'ConsoleAddr is the remote console address.

          '
        in: query
        name: consoleaddr
        schema:
          type: string
      - description: 'Interpreter is a custom interpreter set by the user to run the defender entrypoint script.

          '
        in: query
        name: interpreter
        schema:
          type: string
      - description: 'FilesystemMonitoring is the filesystem monitoring flag.

          '
        in: query
        name: filesystemMonitoring
        schema:
          type: boolean
      - description: 'DefenderImage is the full path to the Defender image, if not specified Prisma''s private registry is used.

          '
        in: query
        name: defenderImage
        schema:
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/-_uint8'
          description: ''
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: post-defenders-cloud-run.yaml
      summary: ' Cloud Run Service Y A M L'
  /api/v34.04/defenders/daemonset.yaml:
    post:
      description: 'Generate Daemonset Deployment YAML File. POST /api/v34.04/defenders/daemonset.yaml on the Defenders API. Takes an optional request body. Documented responses: 200.'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/common.DaemonSetOptions'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/-_uint8'
          description: ''
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: post-defenders-daemonset.yaml
      summary: Generate Daemonset Deployment YAML File
      x-description-source: desc/defenders/daemonset_yaml_post.md
  /api/v34.04/defenders/download:
    get:
      description: 'Download Deployed Defenders. GET /api/v34.04/defenders/download on the Defenders API. Takes 18 query parameters. Documented responses: 200.'
      parameters:
      - description: 'Offsets the result to a specific report count. Offset starts from 0.

          '
        in: query
        name: offset
        schema:
          type: integer
      - description: 'Limit is the amount to fix.

          '
        in: query
        name: limit
        schema:
          type: integer
      - description: 'Sorts the result using a key.

          '
        in: query
        name: sort
        schema:
          type: string
      - description: 'Sorts the result in reverse order.

          '
        in: query
        name: reverse
        schema:
          type: boolean
      - description: 'Hostname is a name of a specific Defender to retrieve.

          '
        in: query
        name: hostname
        schema:
          type: string
      - description: 'Roles are the defender api.Roles to filter.

          '
        in: query
        name: role
        schema:
          description: 'Roles are the defender api.Roles to filter.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Indicates whether to return only connected Defenders (true) or disconnected Defenders (false).

          '
        in: query
        name: connected
        schema:
          type: boolean
      - description: 'Indicates the Defender types to return (e.g., docker, dockerWindows, cri, etc).

          '
        in: query
        name: type
        schema:
          description: 'Indicates the Defender types to return (e.g., docker, dockerWindows, cri, etc).

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Indicates whether to return a list of Defenders that are running the latest version of Prisma Cloud (true)

          or defenders with older versions (false).

          '
        in: query
        name: latest
        schema:
          type: boolean
      - description: 'SupportedVersion indicates only Defenders of supported versions should be fetched.

          '
        in: query
        name: supportedVersion
        schema:
          type: boolean
      - description: 'Scopes the query by cluster name.

          '
        in: query
        name: cluster
        schema:
          description: 'Scopes the query by cluster name.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Scopes the query by TAS cluster IDs.

          '
        in: query
        name: tasClusterIDs
        schema:
          description: 'Scopes the query by TAS cluster IDs.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Scopes the query by TAS blobstore scanning only Defenders (true) or TAS full coverage Defenders (false).

          '
        in: query
        name: tasBlobstoreScanner
        schema:
          type: boolean
      - description: 'Scopes the query by TAS foundations.

          '
        in: query
        name: tasFoundations
        schema:
          description: 'Scopes the query by TAS foundations.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Scopes the query to defenders which are using old certificate.

          '
        in: query
        name: usingOldCA
        schema:
          type: boolean
      - description: 'Scopes the query to defenders which are using expired certificate.

          '
        in: query
        name: usingExpiredCA
        schema:
          type: boolean
      - description: 'Scopes the query by provider type

          Indicates whether to return only defenders running on ARM64 architecture.

          '
        in: query
        name: isARM64
        schema:
          type: boolean
      - description: 'Indicates whether to return only defenders running as VPC Observer.

          '
        in: query
        name: isVPCObserver
        schema:
          type: boolean
      responses:
        '200':
          description: OK
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: get-defenders-download
      summary: Download Deployed Defenders
      x-description-source: desc/defenders/download_get.md
  /api/v34.04/defenders/eks-fargate.yaml:
    post:
      description: 'GenerateK8sControllerManifest receives a Kubernetes controller definition in YAML format,

        augments the YAML to include defenders, and returns a new YAML.

        '
      parameters:
      - description: 'ConsoleAddr is the remote console address.

          '
        in: query
        name: consoleaddr
        schema:
          type: string
      - description: 'Interpreter is a custom interpreter set by the user to run the defender entrypoint script.

          '
        in: query
        name: interpreter
        schema:
          type: string
      - description: 'FilesystemMonitoring is the filesystem monitoring flag.

          '
        in: query
        name: filesystemMonitoring
        schema:
          type: boolean
      - description: 'DefenderImage is the full path to the Defender image, if not specified Prisma''s private registry is used.

          '
        in: query
        name: defenderImage
        schema:
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/-_uint8'
          description: ''
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: post-defenders-eks-fargate.yaml
      summary: ' Generate K8s Controller Manifest'
  /api/v34.04/defenders/fargate.json:
    post:
      description: 'Generate a Protected JSON Fargate Task Definition. POST /api/v34.04/defenders/fargate.json on the Defenders API. Takes 10 query parameters, an optional request body. Documented responses: 200.'
      parameters:
      - description: 'ConsoleAddr is the remote console address.

          '
        in: query
        name: consoleaddr
        schema:
          type: string
      - description: 'DefenderType is the type of the defender to create the install bundle for.

          '
        in: query
        name: defenderType
        schema:
          description: Type is the type to be given at startup
          enum:
          - - none
            - docker
            - dockerWindows
            - containerdWindows
            - swarm
            - daemonset
            - serverLinux
            - serverWindows
            - cri
            - fargate
            - appEmbedded
            - tas
            - tasWindows
            - serverless
            - ecs
            - podman
            - eksFargate
          type: string
      - description: 'Interpreter is a custom interpreter set by the user to run the fargate defender entrypoint script.

          '
        in: query
        name: interpreter
        schema:
          type: string
      - description: 'CloudFormation indicates if the given fargate task definition is in Cloud Formation format.

          '
        in: query
        name: cloudFormation
        schema:
          type: boolean
      - description: 'FilesystemMonitoring is the filesystem monitoring flag.

          '
        in: query
        name: filesystemMonitoring
        schema:
          type: boolean
      - description: 'ExtractEntrypoint indicates if entrypoint will be extracted automatically.

          '
        in: query
        name: extractEntrypoint
        schema:
          type: boolean
      - description: 'RegistryType is the registry type for fetching image details needed to create fargate task definition (e.g., dockerhub).

          '
        in: query
        name: registryType
        schema:
          type: string
      - description: 'RegistryCredentialID of the credentials in the credentials store to use for authenticating with the registry.

          '
        in: query
        name: registryCredentialID
        schema:
          type: string
      - description: 'DefenderImage is the full path to the Defender image, if not specified Prisma''s private registry is used.

          '
        in: query
        name: defenderImage
        schema:
          type: string
      - description: 'DefenderImagePullSecret is the name of the secret required to pull the Defender image from private registry.

          '
        in: query
        name: defenderImagePullSecret
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/appembedded.FargateTask'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/appembedded.FargateTask'
          description: FargateTask represents the generic fargate task AWS template
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: post-defenders-fargate.json
      summary: Generate a Protected JSON Fargate Task Definition
      x-description-source: desc/defenders/fargate_json_post.md
  /api/v34.04/defenders/fargate.yaml:
    post:
      description: 'Generate a Protected YAML Fargate Task Definition. POST /api/v34.04/defenders/fargate.yaml on the Defenders API. Takes 10 query parameters. Documented responses: 200.'
      parameters:
      - description: 'ConsoleAddr is the remote console address.

          '
        in: query
        name: consoleaddr
        schema:
          type: string
      - description: 'DefenderType is the type of the defender to create the install bundle for.

          '
        in: query
        name: defenderType
        schema:
          description: Type is the type to be given at startup
          enum:
          - - none
            - docker
            - dockerWindows
            - containerdWindows
            - swarm
            - daemonset
            - serverLinux
            - serverWindows
            - cri
            - fargate
            - appEmbedded
            - tas
            - tasWindows
            - serverless
            - ecs
            - podman
            - eksFargate
          type: string
      - description: 'Interpreter is a custom interpreter set by the user to run the fargate defender entrypoint script.

          '
        in: query
        name: interpreter
        schema:
          type: string
      - description: 'CloudFormation indicates if the given fargate task definition is in Cloud Formation format.

          '
        in: query
        name: cloudFormation
        schema:
          type: boolean
      - description: 'FilesystemMonitoring is the filesystem monitoring flag.

          '
        in: query
        name: filesystemMonitoring
        schema:
          type: boolean
      - description: 'ExtractEntrypoint indicates if entrypoint will be extracted automatically.

          '
        in: query
        name: extractEntrypoint
        schema:
          type: boolean
      - description: 'RegistryType is the registry type for fetching image details needed to create fargate task definition (e.g., dockerhub).

          '
        in: query
        name: registryType
        schema:
          type: string
      - description: 'RegistryCredentialID of the credentials in the credentials store to use for authenticating with the registry.

          '
        in: query
        name: registryCredentialID
        schema:
          type: string
      - description: 'DefenderImage is the full path to the Defender image, if not specified Prisma''s private registry is used.

          '
        in: query
        name: defenderImage
        schema:
          type: string
      - description: 'DefenderImagePullSecret is the name of the secret required to pull the Defender image from private registry.

          '
        in: query
        name: defenderImagePullSecret
        schema:
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/-_uint8'
          description: ''
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: post-defenders-fargate.yaml
      summary: Generate a Protected YAML Fargate Task Definition
      x-description-source: desc/defenders/fargate_yaml_post.md
  /api/v34.04/defenders/helm/twistlock-defender-helm.tar.gz:
    post:
      description: 'Generate a Helm Deployment Chart for Defender. POST /api/v34.04/defenders/helm/twistlock-defender-helm.tar.gz on the Defenders API. Takes an optional request body. Documented responses: 200.'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/common.DaemonSetOptions'
      responses:
        '200':
          description: OK
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: post-defenders-helm-twistlock-defender-helm.tar.gz
      summary: Generate a Helm Deployment Chart for Defender
      x-description-source: desc/defenders/helm_post.md
  /api/v34.04/defenders/image-name:
    get:
      description: 'Get Docker Image Name for Defender. GET /api/v34.04/defenders/image-name on the Defenders API. Documented responses: 200.'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/string'
          description: ''
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: get-defenders-image-name
      summary: Get Docker Image Name for Defender
      x-description-source: desc/defenders/image-name_get.md
  /api/v34.04/defenders/install-bundle:
    get:
      description: 'Get Certificate Bundle for Defender. GET /api/v34.04/defenders/install-bundle on the Defenders API. Takes 10 query parameters. Documented responses: 200.'
      parameters:
      - description: 'ConsoleAddr is the remote console address.

          '
        in: query
        name: consoleaddr
        schema:
          type: string
      - description: 'DefenderType is the type of the defender to create the install bundle for.

          '
        in: query
        name: defenderType
        schema:
          description: Type is the type to be given at startup
          enum:
          - - none
            - docker
            - dockerWindows
            - containerdWindows
            - swarm
            - daemonset
            - serverLinux
            - serverWindows
            - cri
            - fargate
            - appEmbedded
            - tas
            - tasWindows
            - serverless
            - ecs
            - podman
            - eksFargate
          type: string
      - description: 'Interpreter is a custom interpreter set by the user to run the fargate defender entrypoint script.

          '
        in: query
        name: interpreter
        schema:
          type: string
      - description: 'CloudFormation indicates if the given fargate task definition is in Cloud Formation format.

          '
        in: query
        name: cloudFormation
        schema:
          type: boolean
      - description: 'FilesystemMonitoring is the filesystem monitoring flag.

          '
        in: query
        name: filesystemMonitoring
        schema:
          type: boolean
      - description: 'ExtractEntrypoint indicates if entrypoint will be extracted automatically.

          '
        in: query
        name: extractEntrypoint
        schema:
          type: boolean
      - description: 'RegistryType is the registry type for fetching image details needed to create fargate task definition (e.g., dockerhub).

          '
        in: query
        name: registryType
        schema:
          type: string
      - description: 'RegistryCredentialID of the credentials in the credentials store to use for authenticating with the registry.

          '
        in: query
        name: registryCredentialID
        schema:
          type: string
      - description: 'DefenderImage is the full path to the Defender image, if not specified Prisma''s private registry is used.

          '
        in: query
        name: defenderImage
        schema:
          type: string
      - description: 'DefenderImagePullSecret is the name of the secret required to pull the Defender image from private registry.

          '
        in: query
        name: defenderImagePullSecret
        schema:
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/shared.DefenderInstallBundle'
          description: DefenderInstallBundle represents the install bundle for the defender
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: get-defenders-install-bundle
      summary: Get Certificate Bundle for Defender
      x-description-source: desc/defenders/install-bundle_get.md
  /api/v34.04/defenders/names:
    get:
      description: 'Get Defender Names. GET /api/v34.04/defenders/names on the Defenders API. Takes 18 query parameters. Documented responses: 200.'
      parameters:
      - description: 'Offsets the result to a specific report count. Offset starts from 0.

          '
        in: query
        name: offset
        schema:
          type: integer
      - description: 'Limit is the amount to fix.

          '
        in: query
        name: limit
        schema:
          type: integer
      - description: 'Sorts the result using a key.

          '
        in: query
        name: sort
        schema:
          type: string
      - description: 'Sorts the result in reverse order.

          '
        in: query
        name: reverse
        schema:
          type: boolean
      - description: 'Hostname is a name of a specific Defender to retrieve.

          '
        in: query
        name: hostname
        schema:
          type: string
      - description: 'Roles are the defender api.Roles to filter.

          '
        in: query
        name: role
        schema:
          description: 'Roles are the defender api.Roles to filter.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Indicates whether to return only connected Defenders (true) or disconnected Defenders (false).

          '
        in: query
        name: connected
        schema:
          type: boolean
      - description: 'Indicates the Defender types to return (e.g., docker, dockerWindows, cri, etc).

          '
        in: query
        name: type
        schema:
          description: 'Indicates the Defender types to return (e.g., docker, dockerWindows, cri, etc).

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Indicates whether to return a list of Defenders that are running the latest version of Prisma Cloud (true)

          or defenders with older versions (false).

          '
        in: query
        name: latest
        schema:
          type: boolean
      - description: 'SupportedVersion indicates only Defenders of supported versions should be fetched.

          '
        in: query
        name: supportedVersion
        schema:
          type: boolean
      - description: 'Scopes the query by cluster name.

          '
        in: query
        name: cluster
        schema:
          description: 'Scopes the query by cluster name.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Scopes the query by TAS cluster IDs.

          '
        in: query
        name: tasClusterIDs
        schema:
          description: 'Scopes the query by TAS cluster IDs.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Scopes the query by TAS blobstore scanning only Defenders (true) or TAS full coverage Defenders (false).

          '
        in: query
        name: tasBlobstoreScanner
        schema:
          type: boolean
      - description: 'Scopes the query by TAS foundations.

          '
        in: query
        name: tasFoundations
        schema:
          description: 'Scopes the query by TAS foundations.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Scopes the query to defenders which are using old certificate.

          '
        in: query
        name: usingOldCA
        schema:
          type: boolean
      - description: 'Scopes the query to defenders which are using expired certificate.

          '
        in: query
        name: usingExpiredCA
        schema:
          type: boolean
      - description: 'Scopes the query by provider type

          Indicates whether to return only defenders running on ARM64 architecture.

          '
        in: query
        name: isARM64
        schema:
          type: boolean
      - description: 'Indicates whether to return only defenders running as VPC Observer.

          '
        in: query
        name: isVPCObserver
        schema:
          type: boolean
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/-_string'
          description: ''
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: get-defenders-names
      summary: Get Defender Names
      x-description-source: desc/defenders/names_get.md
  /api/v34.04/defenders/serverless/bundle:
    post:
      description: 'Generate Serverless Bundle for Defender. POST /api/v34.04/defenders/serverless/bundle on the Defenders API. Takes an optional request body. Documented responses: 200.'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/shared.ServerlessBundleRequest'
      responses:
        '200':
          description: OK
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: post-defenders-serverless-bundle
      summary: Generate Serverless Bundle for Defender
      x-description-source: desc/defenders/serverless-bundle_post.md
  /api/v34.04/defenders/summary:
    get:
      description: 'Get Defenders Summary. GET /api/v34.04/defenders/summary on the Defenders API. Documented responses: 200.'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/-_types.DefenderSummary'
          description: ''
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: get-defenders-summary
      summary: Get Defenders Summary
      x-description-source: desc/defenders/summary_get.md
  /api/v34.04/defenders/tas-cloud-controller-address:
    get:
      description: 'Defenders Tas Cloud Controller Address. GET /api/v34.04/defenders/tas-cloud-controller-address on the Defenders API. Takes 18 query parameters. Documented responses: 200.'
      parameters:
      - description: 'Offsets the result to a specific report count. Offset starts from 0.

          '
        in: query
        name: offset
        schema:
          type: integer
      - description: 'Limit is the amount to fix.

          '
        in: query
        name: limit
        schema:
          type: integer
      - description: 'Sorts the result using a key.

          '
        in: query
        name: sort
        schema:
          type: string
      - description: 'Sorts the result in reverse order.

          '
        in: query
        name: reverse
        schema:
          type: boolean
      - description: 'Hostname is a name of a specific Defender to retrieve.

          '
        in: query
        name: hostname
        schema:
          type: string
      - description: 'Roles are the defender api.Roles to filter.

          '
        in: query
        name: role
        schema:
          description: 'Roles are the defender api.Roles to filter.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Indicates whether to return only connected Defenders (true) or disconnected Defenders (false).

          '
        in: query
        name: connected
        schema:
          type: boolean
      - description: 'Indicates the Defender types to return (e.g., docker, dockerWindows, cri, etc).

          '
        in: query
        name: type
        schema:
          description: 'Indicates the Defender types to return (e.g., docker, dockerWindows, cri, etc).

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Indicates whether to return a list of Defenders that are running the latest version of Prisma Cloud (true)

          or defenders with older versions (false).

          '
        in: query
        name: latest
        schema:
          type: boolean
      - description: 'SupportedVersion indicates only Defenders of supported versions should be fetched.

          '
        in: query
        name: supportedVersion
        schema:
          type: boolean
      - description: 'Scopes the query by cluster name.

          '
        in: query
        name: cluster
        schema:
          description: 'Scopes the query by cluster name.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Scopes the query by TAS cluster IDs.

          '
        in: query
        name: tasClusterIDs
        schema:
          description: 'Scopes the query by TAS cluster IDs.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Scopes the query by TAS blobstore scanning only Defenders (true) or TAS full coverage Defenders (false).

          '
        in: query
        name: tasBlobstoreScanner
        schema:
          type: boolean
      - description: 'Scopes the query by TAS foundations.

          '
        in: query
        name: tasFoundations
        schema:
          description: 'Scopes the query by TAS foundations.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Scopes the query to defenders which are using old certificate.

          '
        in: query
        name: usingOldCA
        schema:
          type: boolean
      - description: 'Scopes the query to defenders which are using expired certificate.

          '
        in: query
        name: usingExpiredCA
        schema:
          type: boolean
      - description: 'Scopes the query by provider type

          Indicates whether to return only defenders running on ARM64 architecture.

          '
        in: query
        name: isARM64
        schema:
          type: boolean
      - description: 'Indicates whether to return only defenders running as VPC Observer.

          '
        in: query
        name: isVPCObserver
        schema:
          type: boolean
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/-_string'
          description: ''
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: get-defenders-tas-cloud-controller-address
      summary: Defenders Tas Cloud Controller Address
      x-description-source: desc/defenders/tas-cloud-controller-address_get.md
  /api/v34.04/defenders/upgrade:
    post:
      description: 'Upgrade Connected Single Linux Defenders. POST /api/v34.04/defenders/upgrade on the Defenders API. Takes 18 query parameters. Documented responses: 200.'
      parameters:
      - description: 'Offsets the result to a specific report count. Offset starts from 0.

          '
        in: query
        name: offset
        schema:
          type: integer
      - description: 'Limit is the amount to fix.

          '
        in: query
        name: limit
        schema:
          type: integer
      - description: 'Sorts the result using a key.

          '
        in: query
        name: sort
        schema:
          type: string
      - description: 'Sorts the result in reverse order.

          '
        in: query
        name: reverse
        schema:
          type: boolean
      - description: 'Hostname is a name of a specific Defender to retrieve.

          '
        in: query
        name: hostname
        schema:
          type: string
      - description: 'Roles are the defender api.Roles to filter.

          '
        in: query
        name: role
        schema:
          description: 'Roles are the defender api.Roles to filter.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Indicates whether to return only connected Defenders (true) or disconnected Defenders (false).

          '
        in: query
        name: connected
        schema:
          type: boolean
      - description: 'Indicates the Defender types to return (e.g., docker, dockerWindows, cri, etc).

          '
        in: query
        name: type
        schema:
          description: 'Indicates the Defender types to return (e.g., docker, dockerWindows, cri, etc).

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Indicates whether to return a list of Defenders that are running the latest version of Prisma Cloud (true)

          or defenders with older versions (false).

          '
        in: query
        name: latest
        schema:
          type: boolean
      - description: 'SupportedVersion indicates only Defenders of supported versions should be fetched.

          '
        in: query
        name: supportedVersion
        schema:
          type: boolean
      - description: 'Scopes the query by cluster name.

          '
        in: query
        name: cluster
        schema:
          description: 'Scopes the query by cluster name.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Scopes the query by TAS cluster IDs.

          '
        in: query
        name: tasClusterIDs
        schema:
          description: 'Scopes the query by TAS cluster IDs.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Scopes the query by TAS blobstore scanning only Defenders (true) or TAS full coverage Defenders (false).

          '
        in: query
        name: tasBlobstoreScanner
        schema:
          type: boolean
      - description: 'Scopes the query by TAS foundations.

          '
        in: query
        name: tasFoundations
        schema:
          description: 'Scopes the query by TAS foundations.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
      - description: 'Scopes the query to defenders which are using old certificate.

          '
        in: query
        name: usingOldCA
        schema:
          type: boolean
      - description: 'Scopes the query to defenders which are using expired certificate.

          '
        in: query
        name: usingExpiredCA
        schema:
          type: boolean
      - description: 'Scopes the query by provider type

          Indicates whether to return only defenders running on ARM64 architecture.

          '
        in: query
        name: isARM64
        schema:
          type: boolean
      - description: 'Indicates whether to return only defenders running as VPC Observer.

          '
        in: query
        name: isVPCObserver
        schema:
          type: boolean
      responses:
        '200':
          description: OK
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: post-defenders-upgrade
      summary: Upgrade Connected Single Linux Defenders
      x-description-source: desc/defenders/upgrade_post.md
  /api/v34.04/defenders/{id}:
    delete:
      description: 'Delete a Defender. DELETE /api/v34.04/defenders/{id} on the Defenders API. Takes 1 path parameter. Documented responses: 200.'
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        '200':
          description: OK
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: delete-defenders-id
      summary: Delete a Defender
      x-description-source: desc/defenders/id_delete.md
  /api/v34.04/defenders/{id}/features:
    post:
      description: 'Update Defender Configuration. POST /api/v34.04/defenders/{id}/features on the Defenders API. Takes 1 path parameter, an optional request body. Documented responses: 200.'
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/defender.Features'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/defender.Defender'
          description: Defender is an update about an agent starting
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: post-defenders-id-features
      summary: Update Defender Configuration
      x-description-source: desc/defenders/id_features_post.md
  /api/v34.04/defenders/{id}/restart:
    post:
      description: 'Restart a Defender. POST /api/v34.04/defenders/{id}/restart on the Defenders API. Takes 1 path parameter. Documented responses: 200.'
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        '200':
          description: OK
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: post-defenders-id-restart
      summary: Restart a Defender
      x-description-source: desc/defenders/id_restart_post.md
  /api/v34.04/defenders/{id}/upgrade:
    post:
      description: 'Upgrade a Defender. POST /api/v34.04/defenders/{id}/upgrade on the Defenders API. Takes 1 path parameter. Documented responses: 200.'
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        '200':
          description: OK
        default:
          description: ''
      tags:
      - Defenders
      x-prisma-cloud-target-env:
        permission: manageDefenders
      operationId: post-defenders-id-upgrade
      summary: Upgrade a Defender
      x-description-source: desc/defenders/id_upgrade_post.md
components:
  schemas:
    -_types.DefenderSummary:
      items:
        $ref: '#/components/schemas/types.DefenderSummary'
      type: array
    shared.ServerlessBundleRequest:
      description: ServerlessBundleRequest represents the arguments to serverless bundle request
      properties:
        provider:
          $ref: '#/components/schemas/common.CloudProvider'
        proxyCA:
          description: 'ProxyCA is the proxy’s CA certificate for Defender to trust.

            '
          type: string
        runtime:
          $ref: '#/components/schemas/shared.LambdaRuntimeType'
      type: object
    common.CloudMetadata:
      description: CloudMetadata is the metadata for a cloud provider managed asset (e.g., as part of AWS/GCP/Azure/OCI)
      properties:
        accountID:
          description: 'Cloud account ID.

            '
          type: string
        awsExecutionEnv:
          description: 'AWS execution environment (e.g. EC2/Fargate).

            '
          type: string
        azure:
          $ref: '#/components/schemas/common.AzureMetadata'
        gcp:
          $ref: '#/components/schemas/common.GCPCloudMetadata'
        image:
          description: 'The name of the image the cloud managed host or container is based on.

            '
          type: string
        labels:
          description: 'Cloud provider metadata labels.

            '
          items:
            $ref: '#/components/schemas/common.ExternalLabel'
          type: array
        name:
          description: 'Resource name.

            '
          type: string
        ociTenantID:
          description: 'OCI Tenant ID.

            '
          type: string
        provider:
          $ref: '#/components/schemas/common.CloudProvider'
        region:
          description: 'Resource''s region.

            '
          type: string
        resourceID:
          description: 'Unique ID of the resource.

            '
          type: string
        resourceURL:
          description: 'Server-defined URL for the resource.

            '
          type: string
        type:
          description: 'Instance type.

            '
          type: string
        vmID:
          description: 'Azure unique vm ID.

            '
          type: string
        vmImageID:
          description: 'VMImageID holds the VM instance''s image ID.

            '
          type: string
      type: object
    common.DaemonSetOptions:
      description: DaemonSetOptions are options for creating the daemonset install script for defenders
      properties:
        annotations:
          additionalProperties:
            $ref: '#/components/schemas/string'
          description: 'Annotations is mapping of key-value pairs of annotations metadata - optional.

            '
          type: object
        bottlerocket:
          description: 'Bottlerocket indicates whether to be deployed on a Bottlerocket Linux OS.

            '
          type: boolean
        cluster:
          description: 'Cluster is the kubernetes or ecs cluster name.

            '
          type: string
        clusterNameResolvingMethod:
          description: 'ClusterNameResolvingMethod is the method used to resolve the cluster name, could be default, manual or api-server.

            '
          type: string
        collectPodLabels:
          description: 'CollectPodLabels indicates whether to collect pod related labels resource labels.

            '
          type: boolean
        consoleAddr:
          description: 'ConsoleAddr is the console address for defender communication.

            '
          type: string
        containerRuntime:
          $ref: '#/components/schemas/common.ContainerRuntime'
        cpuLimit:
          description: 'CPULimit is the cpu limit for the defender deamonset - optional.

            '
          type: integer
        credentialID:
          description: 'CredentialID is the name of the credential used.

            '
          type: string
        dockerSocketPath:
          description: 'DockerSocketPath is the path of the docker socket file.

            '
          type: string
        gkeAutopilot:
          description: 'GKEAutopilot indicates the deployment is requested for GKE Autopilot.

            '
          type: boolean
        image:
          description: 'Image is the full daemonset image name.

            '
          type: string
        istio:
          description: 'MonitorIstio indicates whether to monitor Istio.

            '
          type: boolean
        memoryLimit:
          description: 'MemoryLimit is a memory limit for the defender deamonset - optional.

            '
          type: integer
        namespace:
          description: 'Namespace is the target deamonset namespaces.

            '
          type: string
        nftablesEnabled:
          description: 'NftablesEnabled indicates whether defender will use nftables instead of iptable.

            '
          type: boolean
        nodeSelector:
          description: 'NodeSelector is a key/value node selector.

            '
          type: string
        orchestration:
          description: 'Orchestration is the orchestration type.

            '
          type: string
        priorityClassName:
          description: 'PriorityClassName is the name of the priority class for the defender - optional.

            '
          type: string
        privileged:
          description: 'Privileged indicates whether to run defenders as privileged.

            '
          type: boolean
        projectID:
          description: 'ProjectID is the kubernetes cluster project ID.

            '
          type: string
        proxy:
          $ref: '#/components/schemas/common.DefenderProxyOpt'
        region:
          description: 'Region is the kubernetes cluster location region.

            '
          type: string
        roleARN:
          description: 'RoleARN is the role''s ARN to associate with the created service account - optional.

            '
          type: string
        secretsname:
          description: 'SecretsName is the name of the secret to pull.

            '
          type: string
        selinux:
          description: 'SelinuxEnforced indicates whether selinux is enforced on the target host.

            '
          type: boolean
        serviceaccounts:
          description: 'MonitorServiceAccounts indicates whether to monitor service accounts.

            '
          type: boolean
        talos:
          description: 'Talos indicates if the daemonset is to be deployed on a Talos Linux k8s cluster.

            '
          type: boolean
        tolerations:
          description: 'Tolerations is a list of tolerations for the defender deamonset - optional.

            '
          items:
            $ref: '#/components/schemas/common.Toleration'
          type: array
        uniqueHostname:
          description: 'UniqueHostname indicates whether to assign unique hostnames.

            '
          type: boolean
      type: object
    common.ContainerRuntime:
      description: ContainerRuntime represents the supported container runtime types
      enum:
      - - docker
        - containerd
        - crio
      type: string
    defender.Status:
      description: Status is the generic status state per defender or global
      properties:
        appFirewall:
          $ref: '#/components/schemas/defender.FeatureStatus'
        container:
          $ref: '#/components/schemas/defender.ScanStatus'
        containerNetworkFirewall:
          $ref: '#/components/schemas/defender.FeatureStatus'
        features:
          $ref: '#/components/schemas/defender.FeatureStatus'
        filesystem:
          $ref: '#/components/schemas/defender.FeatureStatus'
        hostCustomCompliance:
          $ref: '#/components/schemas/defender.FeatureStatus'
        hostNetworkFirewall:
          $ref: '#/components/schemas/defender.FeatureStatus'
        image:
          $ref: '#/components/schemas/defender.ScanStatus'
        lastModified:
          description: 'Datetime the status was last modified.

            '
          format: date-time
          type: string
        network:
          $ref: '#/components/schemas/defender.FeatureStatus'
        outOfBandAppFirewall:
          $ref: '#/components/schemas/defender.FeatureStatus'
        process:
          $ref: '#/components/schemas/defender.FeatureStatus'
        runc:
          $ref: '#/components/schemas/defender.FeatureStatus'
        runtime:
          $ref: '#/components/schemas/defender.FeatureStatus'
        tasDroplets:
          $ref: '#/components/schemas/defender.ScanStatus'
        upgrade:
          $ref: '#/components/schemas/defender.UpgradeStatus'
      type: object
    waas.UnprotectedProcess:
      description: UnprotectedProcess holds unprotected processes alongside the port
      properties:
        port:
          description: 'Port is the process port.

            '
          type: integer
        process:
          description: 'Process is the process name.

            '
          type: string
        tls:
          description: 'TLS is the port TLS indication.

            '
          type: boolean
      type: object
    appembedded.FargateTask:
      additionalProperties:
        $ref: '#/components/schemas/string'
      description: FargateTask represents the generic fargate task AWS template
      type: object
    common.ACIMetadata:
      properties:
        containerGroup:
          description: '.

            '
          type: string
      type: object
    defender.ScanStatus:
      description: ScanStatus represents the status of current scan
      properties:
        completed:
          description: 'Indicates if scanning has successfully completed (true) or not (false).

            '
          type: boolean
        errors:
          description: 'List of errors that occurred during the last scan.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
        hostname:
          description: 'Name of the host where Defender runs.

            '
          type: string
        scanTime:
          description: 'Datetime of the last completed scan.

            '
          format: date-time
          type: string
        scanning:
          description: 'Indicates whether scanning is in progress (true) or not (false).

            '
          type: boolean
        selective:
          description: 'Indicates if the scan is for a specific resource (true) or not (false).

            '
          type: boolean
      type: object
    defender.Category:
      description: Category represents the defender target category
      enum:
      - - container
        - host
        - serverless
        - appEmbedded
        - hostAgentless
        - containerAgentless
        - cloudSecurityAgent
      type: string
    common.CloudRunMetadata:
      properties:
        revision:
          description: '.

            '
          type: string
        service:
          description: '.

            '
          type: string
      type: object
    common.GCPCloudMetadata:
      properties:
        cloudRun:
          $ref: '#/components/schemas/common.CloudRunMetadata'
      type: object
    common.CloudProvider:
      description: CloudProvider specifies the cloud provider name
      enum:
      - - aws
        - azure
        - gcp
        - alibaba
        - oci
        - others
      type: string
    common.ExternalLabel:
      description: ExternalLabel holds an external label with a source and timestamp
      properties:
        key:
          description: 'Label key.

            '
          type: string
        sourceName:
          description: 'Source name (e.g., for a namespace, the source name can be ''twistlock'').

            '
          type: string
        sourceType:
          $ref: '#/components/schemas/common.ExternalLabelSourceType'
        timestamp:
          description: 'Time when the label was fetched.

            '
          format: date-time
          type: string
        value:
          description: 'Value of the label.

            '
          type: string
      type: object
    common.Toleration:
      description: 'Toleration holds options for pod toleration

        ref: https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/

        code ref: k8s.io/api/core/v1/types.go'
      properties:
        effect:
          description: 'Effect indicates the taint effect to match. Empty means match all taint effects.

            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.

            +optional.

            '
          type: string
        key:
          description: 'Key is the taint key that the toleration applies to. Empty means match all taint keys.

            If the key is empty, operator must be Exists; this combination means to match all values and all keys.

            +optional.

            '
          type: string
        operator:
          description: 'Operator represents a key''s relationship to the value.

            Valid operators are Exists and Equal. Defaults to Equal.

            Exists is equivalent to wildcard for value, so that a pod can

            tolerate all taints of a particular category.

            +optional.

            '
          type: string
        tolerationSeconds:
          description: 'TolerationSeconds represents the period of time the toleration (which must be

            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,

            it is not set, which means tolerate the taint forever (do not evict). Zero and

            negative values will be treated as 0 (evict immediately) by the system.

            +optional.

            '
          format: int64
          type: integer
        value:
          description: 'Value is the taint value the toleration matches to.

            If the operator is Exists, the value should be empty, otherwise just a regular string.

            +optional.

            '
          type: string
      type: object
    string:
      type: string
    common.Secret:
      description: Secret Stores the plain and encrypted version of a value. The plain version is not stored in a database
      properties:
        encrypted:
          description: 'Specifies an encrypted value of the secret.

            '
          type: string
        plain:
          description: 'Specifies the plain text value of the secret.

            '
          type: string
      type: object
    -_uint8:
      items:
        $ref: '#/components/schemas/uint8'
      type: array
    defender.Features:
      description: Features is the defender features that can be updated
      properties:
        clusterMonitoring:
          description: 'Indicates whether any of the cluster monitoring features are enabled (monitor service accounts, monitor Istio, collect Kubernetes pod labels).

            '
          type: boolean
        proxyListenerType:
          $ref: '#/components/schemas/defender.ProxyListenerType'
      type: object
    defender.FeatureStatus:
      description: FeatureStatus holds data about defender features
      properties:
        enabled:
          description: 'Indicates if the feature is enabled (true) or not (false).

            '
          type: boolean
        err:
          description: 'Error string, if an error occurred.

            '
          type: string
        hostname:
          description: 'Name of host where Defender runs.

            '
          type: string
      type: object
    -_defender.Defender:
      items:
        $ref: '#/components/schemas/defender.Defender'
      type: array
    shared.LambdaRuntimeType:
      description: 'LambdaRuntimeType represents the runtime type of the serverless function

        The constants used are taken from: https://docs.aws.amazon.com/lambda/latest/dg/API_CreateFunction.html#SSS-CreateFunction-request-Runtime'
      enum:
      - - python
        - python3.9
        - python3.10
        - python3.11
        - python3.12
        - python3.13
        - nodejs
        - nodejs18.x
        - nodejs20.x
        - nodejs22.x
        - dotnet
        - dotnetcore2.1
        - dotnetcore3.1
        - dotnet6
        - java
        - java8
        - java11
        - java17
        - java21
        - ruby
      type: string
    types.DefenderSummary:
      description: DefenderSummary is a summary for a type of defender
      properties:
        category:
          $ref: '#/components/schemas/defender.Category'
        connected:
          description: 'Connected counts how many defenders are connected for this category.

            '
          type: integer
        deployed:
          description: 'Deployed counts how many defenders are deployed for this category.

            '
          type: integer
        licensed:
          description: 'Licensed counts how many defenders are licensed for this category.

            '
          type: integer
      type: object
    defender.ProxyListenerType:
      description: ProxyListenerType is the proxy listener type of defenders
      type: string
    common.DefenderProxyOpt:
      description: 'DefenderProxyOpt holds options for defender proxy configuration

        It embeds ProxySettings but override it''s Password field with a simple string

        This is needed in order to avoid Secret''s MarshalJSON method, which depends on existence of master key file'
      properties:
        ca:
          description: 'Proxy''s CA for Defender to trust. Required when using TLS intercept proxies.

            '
          type: string
        httpProxy:
          description: 'Proxy address.

            '
          type: string
        noProxy:
          description: 'List of addresses for which the proxy should not be used.

            '
          type: string
        password:
          description: '.

            '
          type: string
        user:
          description: 'Username to authenticate with the proxy.

            '
          type: string
      type: object
    waas.ProtectionStatus:
      description: ProtectionStatus describes the status of the WAAS protection
      properties:
        enabled:
          description: 'Enabled indicates if WAAS proxy protection is enabled (true) or not (false).

            '
          type: boolean
        outOfBandMode:
          $ref: '#/components/schemas/waas.OutOfBandMode'
        ports:
          description: 'Ports indicates http open ports associated with the container.

            '
          items:
            $ref: '#/components/schemas/int'
          type: array
        supported:
          description: 'Supported indicates if WAAS protection is supported (true) or not (false).

            '
          type: boolean
        tlsPorts:
          description: 'TLSPorts indicates https open ports associated with the container.

            '
          items:
            $ref: '#/components/schemas/int'
          type: array
        unprotectedProcesses:
          description: 'UnprotectedProcesses holds the processes that support HTTP/HTTPS without WAAS protection.

            '
          items:
            $ref: '#/components/schemas/waas.UnprotectedProcess'
          type: array
      type: object
    common.ExternalLabelSourceType:
      description: ExternalLabelSourceType indicates the source of the labels
      enum:
      - - namespace
        - deployment
        - pod
        - aws
        - azure
        - gcp
        - oci
      type: string
    defender.Type:
      description: Type is the type to be given at startup
      enum:
      - - none
        - docker
        - dockerWindows
        - containerdWindows
        - swarm
        - daemonset
        - serverLinux
        - serverWindows
        - cri
        - fargate
        - appEmbedded
        - tas
        - tasWindows
        - serverless
        - ecs
        - podman
        - eksFargate
      type: string
    common.ClusterType:
      description: ClusterType is the cluster type
      enum:
      - - AKS
        - ECS
        - EKS
        - GKE
        - Kubernetes
      type: string
    -_string:
      items:
        $ref: '#/components/schemas/string'
      type: array
    uint8:
      type: integer
    waas.OutOfBandMode:
      description: OutOfBandMode holds the app firewall out-of-band mode
      enum:
      - - ''
        - Observation
        - Protection
      type: string
    shared.DefenderInstallBundle:
      description: DefenderInstallBundle represents the install bundle for the defender
      properties:
        installBundle:
          description: 'InstallBundle is the base64 bundle of certificates used to communicate with the console.

            '
          type: string
        wsAddress:
          description: 'WSAddress is the websocket address (console ) the TAS defender connects to.

            '
          type: string
      type: object
    defender.SystemInfo:
      description: SystemInfo is the OS information of the host
      properties:
        cpuCount:
          description: 'CPU count on the host where Defender runs.

            '
          type: integer
        freeDiskSpaceGB:
          description: 'Free disk space (in GB) on the host where Defender runs.

            '
          type: integer
        kernelVersion:
          description: 'Kernel version on the host where Defender runs.

            '
          type: string
        memoryGB:
          description: 'Total memory (in GB) on the host where Defender runs.

            '
          format: double
          type: number
        totalDiskSpaceGB:
          description: 'Total disk space (in GB) on the host where Defender runs.

            '
          type: integer
      type: object
    defender.UpgradeStatus:
      description: UpgradeStatus represents the status of current twistlock defender upgrade
      properties:
        err:
          description: 'Error string, if an error occurred.

            '
          type: string
        hostname:
          description: 'Name of the host where Defender runs.

            '
          type: string
        lastModified:
          description: 'Datetime of the last upgrade.

            '
          format: date-time
          type: string
        progress:
          description: 'Upgrade progress.

            '
          type: integer
      type: object
    int:
      type: integer
    common.ProxySettings:
      description: ProxySettings are the http proxy settings
      properties:
        ca:
          description: 'Proxy''s CA for Defender to trust. Required when using TLS intercept proxies.

            '
          type: string
        httpProxy:
          description: 'Proxy address.

            '
          type: string
        noProxy:
          description: 'List of addresses for which the proxy should not be used.

            '
          type: string
        password:
          $ref: '#/components/schemas/common.Secret'
        user:
          description: 'Username to authenticate with the proxy.

            '
          type: string
      type: object
    common.AzureMetadata:
      properties:
        aci:
          $ref: '#/components/schemas/common.ACIMetadata'
        resourceGroup:
          description: '.

            '
          type: string
      type: object
    defender.Defender:
      description: Defender is an update about an agent starting
      properties:
        category:
          $ref: '#/components/schemas/defender.Category'
        certificateExpiration:
          description: 'Client certificate expiration time.

            '
          format: date-time
          type: string
        cloudMetadata:
          $ref: '#/components/schemas/common.CloudMetadata'
        cluster:
          description: 'Cluster name (fallback is internal IP).

            '
          type: string
        clusterID:
          description: 'Unique ID generated for each DaemonSet. Used to group Defenders by clusters. Note: Kubernetes does not provide a cluster name as part of its API.

            '
          type: string
        clusterOIDCProviderURL:
          description: 'ClusterOIDCProviderURL is the URL of the OpenID Connect (OIDC) identity provider of the associated Kubernetes cluster.

            '
          type: string
        clusterType:
          $ref: '#/components/schemas/common.ClusterType'
        collections:
          description: 'Collections to which this Defender belongs.

            '
          items:
            $ref: '#/components/schemas/string'
          type: array
        compatibleVersion:
          description: 'Indicates if Defender has a compatible version for communication (e.g., request logs) (true) or not (false).

            '
          type: boolean
        connected:
          description: 'Indicates whether Defender is connected (true) or not (false).

            '
          type: boolean
        container:
          description: 'Container is the container name for app embedded defenders.

            '
          type: string
        features:
          $ref: '#/components/schemas/defender.Features'
        firewallProtection:
          $ref: '#/components/schemas/waas.ProtectionStatus'
        fqdn:
          description: 'Full domain name of the host. Used in audit alerts to identify specific hosts.

            '
          type: string
        hostname:
          description: 'Name of host where Defender is deployed.

            '
          type: string
        isARM64:
          description: 'IsARM64 indicates whether the defender runs on aarch64 architecture.

            '
          type: boolean
        lastModified:
          description: 'Datetime when the Defender''s connectivity status last changed.

            '
          format: date-time
          type: string
        podUid:
          description: 'PodUID is the UID of the pod where the defender is running, the UID is unique within a Kubernetes cluster.

            '
          type: string
        port:
          description: 'Port that Defender uses to connect to Console.

            '
          type: integer
        proxy:
          $ref: '#/components/schemas/common.ProxySettings'
        remoteLoggingSupported:
          description: 'Indicates if Defender logs can be retrieved remotely (true) or not (false).

            '
          type: boolean
        remoteMgmtSupported:
          description: 'Indicates if Defender can be remotely managed (upgraded, restarted) (true) or not (false).

            '
          type: boolean
        status:
          $ref: '#/components/schemas/defender.Status'
        systemInfo:
          $ref: '#/components/schemas/defender.SystemInfo'
        tasBlobstoreScanner:
          description: 'Indicates TAS blobstore scanning only Defender.

            '
          type: boolean
        tasClusterID:
          description: 'TAS cluster ID where Defender runs. This is typically set to the Cloud controller''s API address.

            '
          type: string
        tasFoundation:
          description: 'TASFoundation is the foundation the Defender is running on.

            '
          type: string
        type:
          $ref: '#/components/schemas/defender.Type'
        usingOldCA:
          description: 'UsingOldCA indicates whether the defender client is using an old certificate signed by an old CA for TLS handshake.

            '
          type: boolean
        version:
          description: 'Defender version.

            '
          type: string
        vpcObserver:
          description: 'VPCObserver indicates whether the defender runs in a VPC observer.

            '
          type: boolean
      type: object
    shared.AppEmbeddedEmbedRequest:
      description: AppEmbeddedEmbedRequest represents the arguments required for a AppEmbedded defender embed request
      properties:
        appID:
          description: 'AppID identifies the app that the embedded app defender defender is protecting.

            '
          type: string
        consoleAddr:
          description: 'ConsoleAddr is the console address.

            '
          type: string
        dataFolder:
          description: 'DataFolder is the path to the Twistlock data folder in the container.

            '
          type: string
        dockerfile:
          description: 'Dockerfile is the Dockerfile to embed AppEmbedded defender into.

            '
          type: string
        filesystemMonitoring:
          description: 'FilesystemMonitoring is the flag of filesystem monitoring for this Defender.

            '
          type: boolean
      type: object
x-refined-from:
- palo-alto-compute-34-03-openapi-34-03-138-sh-openapi.json
- palo-alto-compute-openapi-34-04-145-sh-openapi.json
- palo-alto-cwpp-34-03-openapi-34-03-138-saas-openapi.json
- palo-alto-cwpp-openapi-34-04-145-saas-openapi.json
